# NovelNexus V3 架构设计

## 一、版本演进回顾

### V1（`novel-writer`）—— 过度设计的失败版本

**架构**：LangGraph 多 Agent 管道 `Planner → Writer → Reviewer → Reader → Updater`

**致命缺陷**：
- 5 个 Agent 各自只有部分信息视野（Planner 看不到 Writer 的正文，Reader 没有世界观全貌），信息割裂导致各行其是
- 链路过长，一个章节经过 5 次 LLM 调用，延迟巨大、成本爆炸、失败模式组合爆炸
- 各 Agent 之间通过 state dict 传递信息，像传话游戏一样逐级丢失语义
- 空间和时间信息分散在 5 个 Agent 之间，没有一个 Agent 拥有完整时空地图
- Reviewer 说"要改"但 Writer 不理解为什么，Reader 说"体验差"但没办法精确定位
- 只有人类作者才有权做审美判断，多 Agent 评审是伪需求

### V2（`NovelNexus_V2`）—— 吸收教训后的正确方向

**架构**：单 Agent + 大 Prompt，一次调用写一章

**正确之处**：
- 信息完全可见：一个 Agent 看到所有知识，不再传话失真
- 极简：一次调用一章，O(1) 复杂度
- 前缀缓存利用：稳定内容在前，最大化 DeepSeek 缓存命中
- 状态管理干净：`state.json` + 摘要链 + 快照/回滚
- 轻量级提取模型（flash）自动更新状态，分离"创作"和"整理"

**仍然缺失的**：
- 交互模式是单向的：用户给方向 → AI 写一章，没有讨论能力
- `user_direction` 只是一个字符串塞进 prompt，没有讨论、收敛、AI 主动提供替代方案
- 本质上是"高级 autocomplete"，不是"和你一起讨论剧情的同事"
- 用户有想法时需要自己去改知识文件，AI 不参与"想法→讨论→决策→落笔"的过程

---

## 二、V3 核心洞察

**你需要的不只是一个写手，而是一个能和你讨论剧本的创意合伙人。**

```
V1/V2 模式：
  用户指令 → [AI黑箱] → 产出章节

V3 模式：
  用户想法 → [讨论] → [收敛] → [落笔到知识文件] → [基于新知识写下一章]
              ↑_____________________________↓
                  可以多轮迭代
```

---

## 三、核心原则

1. **一个"小说家"Agent** —— 保留 V2 单 Agent 教训，但有两种工作模式（讨论/写作）
2. **知识 = 源文件** —— 世界观、角色、大纲是 markdown 文件，AI 和人类都可以读写
3. **讨论有记忆** —— 讨论历史被结构化保存，AI 知道"我们上次决定了什么"
4. **讨论时谨慎，落笔后慎重反悔** —— 讨论模式充分讨论再修改知识文件，设定一旦进入正文无法无损剥离
5. **AI 是"活生生的小说家"** —— 被动响应为主，但写完一章后可以主动提醒潜在矛盾

---

## 四、架构决策

| 决策 | 选项 | 结论 |
|------|------|------|
| Agent 数量 | A: 1个Agent双模式 / B: 2个独立Agent | **A**：共享知识，讨论历史自然转入写作上下文 |
| 知识格式 | A: Markdown / B: JSON/YAML / C: 混合 | **C**：Markdown（人类可读）+ state.json（动态索引） |
| 讨论深度 | A: 纯被动 / B: 被动+轻度主动 | **B**：写完一章后主动提醒潜在矛盾，但不主动修改 |

---

## 五、文件布局

```
NovelNexus_V3/
├── config.py                    # 配置（API key、模型选择、字数限制等）
├── novelist.py                  # 核心：小说家 Agent（唯一 Agent，双模式）
├── knowledge/
│   ├── 世界观.md                # 用户撰写，AI 可在讨论后修改
│   ├── 角色.md                  # 同上
│   ├── 大纲.md                  # 同上（卷级弧线 + 里程碑，不绑章节号）
│   ├── 风格.md                  # 写作风格（可切换）
│   ├── 续写规范.md              # 续写硬约束
│   ├── state.json               # 动态状态（进度、位置、时间线、伏笔、悬念）
│   ├── summaries.json           # 章节摘要链（卷级压缩后含卷摘要）
│   ├── discussion_log.json      # 讨论记录（结构化）
│   └── snapshots/
│       ├── events.jsonl         # 追加式事件时间线
│       ├── ch_XXX.json          # 章节快照
│       ├── disc_XXX_pre.json    # 讨论前快照（被修改的知识文件副本）
│       └── disc_XXX_post.json   # 讨论后快照
├── output/                      # 生成的章节 markdown
├── system_builder.py            # 组装 system prompt（按缓存稳定性分层）
├── state_manager.py             # 状态管理（加载/保存/快照/回退/摘要链）
├── web_ui.py                    # Web 界面
└── token_tracker.py             # 成本追踪
```

---

## 六、两种工作模式

### 模式 A：写作模式

```
用户："继续写下一章" 或 "继续写下一章，这章重点写..."
AI：  生成章节正文 + Agent 附言（对下一章的建议）→ 提取摘要 → 更新状态 → 输出
用户：可以接受 / 打回重写 / 部分修改

Agent 附言可包含：
- 对下一章节奏的建议
- 潜在矛盾的提醒（"大纲第X章和当前角色状态可能有矛盾，要讨论吗？"）
- 卷切换信号
```

### 模式 B：讨论模式

```
用户："我觉得这个世界应该加一个概念：..."

AI：  ① 分析影响（世界观/角色/大纲/伏笔）
      ② 提供 2-3 个具体方案
      ③ 指出风险和落入俗套的可能性
      ④ 像一个真正的合作者一样给出诚恳的创意判断，不一味附和

用户：选择方案 / 提出修改

AI：  输出结构化"修改清单"（列出需修改的文件和具体内容）

用户：确认

AI：  执行修改 → 创建讨论前快照 → 修改知识文件 → 创建讨论后快照 → 记录 discussion_log
```

**讨论模式的 System Prompt 基调**：
- 创意合伙人 persona
- 完整掌握全部世界观、角色设定和故事大纲
- 给出有主张的建议（"我觉得这个方向更好，因为..."），而非只列选项
- 对一致性负责：发现矛盾时明确指出

---

## 七、时空感知 —— state.json 是唯一真相源

### 设计原理

人类小说家不是在脑子里计算坐标，而是在草稿纸上维护一张"此刻谁在哪"的记录。V3 完全模拟这个做法。

### state.json 核心字段

```json
{
  "next_chapter": 1,
  "novel_title": "小说名",
  "current_volume": {"num": 1, "title": "卷名"},
  "current_scene": "当前场景（如：青藤学院·后勤处走廊）",
  "current_time": "故事内绝对时间（如：第1卷第37天·下午3点）",
  "characters": {
    "角色名": {"location": "位置", "state": "当前状态/动作"}
  },
  "foreshadowing": {
    "pending": [{"item": "伏笔", "status": "状态", "chapter": 0, "keywords": []}],
    "resolved": [{"item": "伏笔", "chapter": 0}]
  },
  "suspense": [{"content": "悬念", "chapter": 0, "resolved": false}],
  "current_suspense": "当前主要悬念的一句话描述",
  "timeline_events": [
    {"event": "事件", "when": "时间锚点", "chapter": 0}
  ],
  "volume_history": [
    {"num": 1, "title": "卷名", "summary": "500字卷摘要", "chapters": "1-50"}
  ],
  "章节悬念类型历史": ["类型1", "类型2"]
}
```

### 时空闭环

```
写作前：完整时空快照注入到小说家上下文
写作中：小说家在已知坐标下创作
写作后：轻量级提取模型自动更新
  - 所有出场角色的新位置
  - 时间推进量（"次日清晨""三天后""当晚"→自动推算绝对时间）
  - 时间线一致性校验（如有角色说"三天前那场战斗"，自动校验）
下次写作前：又是准确的坐标
```

---

## 八、记忆模型 —— 金字塔压缩

### 设计原理

复制人类小说家的多分辨率记忆结构：

```
          ┌──────────────┐
          │  全文记忆     │  ← 最近 2 章，一字不差
          ├──────────────┤
          │  摘要记忆     │  ← 当前卷的每一章，150 字/章
          ├──────────────┤
          │  卷级记忆     │  ← 旧卷每卷一段话，500 字/卷
          ├──────────────┤
          │  "圣经"记忆   │  ← 世界观/角色/大纲，随时查阅
          └──────────────┘
```

### 为什么最近只放 2 章原文（不是 3-5 章）？

- 第 N-1 章结尾：物理场景、情绪状态、最后一句话的质感 → 需要全文
- 第 N-2 章：上章的完整语境，避免对话语气、节奏突然断裂 → 需要全文
- 第 N-3 至 N-5 章：只需要知道"发生了什么"，不需要逐字质感 → 摘要完全足够
- 少放原文 = 少稀释注意力 = 模型对真正重要的信息更聚焦

---

## 九、注意力布局与上下文组装

### DeepSeek 注意力特性

模型对 prompt 开头和结尾的注意力权重最高，中间区域权重下降（"lost in the middle"）。

### System Prompt 分层（按注意力权重从高到低）

```
SYSTEM MESSAGE：
【注意力最高区 · 开头】
├── ① 写作 persona（我是谁、我的职责）——————————— 不可违反的
├── ② 世界观规则 ———————————————————————————————— 硬约束
├── ③ 角色设定 —————————————————————————————————— 绝对不能错

【注意力稳定区 · 中段前部】
├── ④ 写作风格 + 续写规范 ———————————————————— 质感的底线
├── ⑤ 故事大纲（卷级弧线 + 里程碑）—————————— 方向性参考
├── ⑥ 讨论决策摘要（≤500字）—————————————————— 辅助记忆

【注意力衰减区 · 中段】
├── ⑦ 旧卷摘要（每卷500字）—————————————————— "大概记得"
├── ⑧ 当前卷章节摘要链（150字/章）——————————— 辅助检索线索

【注意力回升区 · 靠近 HUMAN MESSAGE】
├── ⑨ 最近 2 章完整原文 ———————————————————— 衔接质感

HUMAN MESSAGE：
【注意力最高区 · 结尾】
├── ⑩ 时空真相源（state.json 快照）—————————— 最需要精确的
└── ⑪ 用户方向 —————————————————————————————— 此刻最重要的
```

### 上下文预算（按 300 章连载估算）

| 层级 | 内容 | Token 估算 | 注意力区 |
|------|------|-----------|---------|
| ① | 写作 persona | 300 | 最高 |
| ② | 世界观.md | 3,000 | 最高 |
| ③ | 角色.md | 2,000 | 最高 |
| ④ | 风格 + 规范 | 1,500 | 高 |
| ⑤ | 大纲（当前卷） | 1,500 | 中 |
| ⑥ | 讨论决策摘要 | 500 | 中 |
| ⑦ | 旧卷摘要（4卷×500字） | 2,000 | 中低 |
| ⑧ | 当前卷摘要（60章×150字） | 9,000 | 中低 |
| ⑨ | 最近 2 章原文 | 5,000 | 高 |
| ⑩ | 时空状态快照 | 500 | 最高 |
| ⑪ | 用户方向 | 50 | 最高 |
| **合计** | | **~25,000** | |

**25K tokens 在 1M 上下文里不到 3%，注意力衰减问题根本不会触发。**

---

## 十、缓存策略

### DeepSeek 前缀缓存机制

- 从 system message 开头算起，连续的相同内容 = 缓存命中（¥0.2/M tokens）
- 未命中 = 全价输入（¥2/M tokens）
- **差距 10 倍**

### 缓存分层

```
【第 0 层：永久固化层】          缓存：100% 命中
├── 写作 persona
├── 续写规范
└── 风格文件
    → 永远不变，永远命中，零成本

【第 1 层：知识固件层】          缓存：99%+ 命中
├── 世界观.md（只在讨论后修改）
├── 角色.md（只在讨论后修改）
└── 大纲.md（只在讨论后修改）
    → 写 20 章才可能改一次，几乎永远命中

【第 2 层：渐进记忆层】          缓存：尾部未命中
├── 章节摘要链（前 N-1 章命中，最后一章新增）
└── 讨论日志摘要（前 N-1 条命中，最后一条新增）
    → 只有最后约 150 字不命中，其余全命中
```

### 三条铁律

1. **稳定内容绝不在 human message 里重复**（会导致后续内容全部缓存失效）
2. **可变内容必须放在最后**（章节号、用户方向→human message）
3. **知识文件修改后仅该层缓存失效一次**（之后写 20 章又全部命中）

### 单章成本估算

| 区域 | Token 量 | 单价 | 成本 |
|------|---------|------|------|
| System（缓存命中） | 15,000 | ¥0.2/M | ~¥0.003 |
| System（缓存未命中） | 500 | ¥2.0/M | ~¥0.001 |
| Human（冷启动） | 8,000 | ¥2.0/M | ~¥0.016 |
| 输出 | 3,000 | ¥3.0/M | ~¥0.009 |
| **单章总成本** | | | **≈ ¥0.029** |

---

## 十一、压缩规则

### 规则 1：卷结束时触发"卷级压缩"

触发条件：卷切换时。将当前卷所有逐章摘要喂给 flash 模型，输出 500 字卷级摘要。

### 规则 2：章节摘要链有硬上限

- 当前卷逐章摘要最多保留最近 80 章
- 超过 80 章自动触发卷级压缩

### 规则 3：讨论日志也需压缩

- 不塞原始对话原文
- 用 flash 提取当前相关决策，输出 ≤ 500 字摘要

### 规则 4：软上限保底

如果 total context 超过 30K tokens：
- 旧卷摘要进一步压缩到 200 字/卷
- 当前卷摘要只保留最近 30 章
- 讨论日志摘要缩减到 200 字
- 大纲只保留当前卷和下一卷

---

## 十二、回退机制

### 设计原则

不搞事件溯源（过度设计），用直观的时间线 + 两步确认。

### 快照体系

```
snapshots/
├── events.jsonl              ← 追加式事件时间线（人类可读）
├── ch_049.json               ← 章节快照（state + summaries）
├── disc_003_pre.json         ← 讨论前快照（知识文件副本）
└── disc_003_post.json        ← 讨论后快照
```

### 回退工作流

```
用户："回到第 49 章之后"
       ↓
系统扫描 events.jsonl → 发现第 49 章后有：
  · disc_003（修改了 世界观.md、角色.md）
  · 第 50 章
       ↓
系统提示影响范围 →
  [仅回退章节] ← 保留讨论修改，只删章节
  [同时撤销讨论] ← 从 disc_003_pre.json 恢复知识文件
       ↓
执行删除 → 恢复状态
```

### UI 呈现

两个按钮覆盖 90% 场景。剩下 10% 精细操作（如只撤销某次讨论但保留章节），用户自行去 snapshots/ 手动恢复文件。

---

## 十三、卷切换自主权

### 设计理念

小说家有权自主决定何时结束当前卷、开启下一卷。人类小说家在写作过程中"感觉弧线走到了尽头"——AI 也应有这个判断力。

### 工作机制

1. 写完每章后，Agent 在附言中自动评估卷级弧线是否已收束
2. 如果判断弧线已收束，输出卷切换信号：`[卷切换] 第N卷「卷名」`
3. 系统识别信号后提示用户确认
4. 用户确认后：
   - 触发卷级压缩
   - 更新 `state.json` 的 `current_volume`
   - 创建快照

### 约束

- 必须确实感觉到弧线收束了才切，不能强切
- 不能仅因为大纲说"该切了"就切，要等故事自己走到那个点
- 卷切换时触发卷级压缩（压缩当前卷所有摘要 → 500 字卷摘要）

---

## 十四、大纲粒度

### 为什么大纲只给粗略路线？

1. **人类小说家的大纲也是粗线条的** —— 知道每一卷的"起点→终点→关键转折"，但不会精确到每一章
2. **详细的大纲是枷锁** —— 如果大纲写到"第 47 章：林野在教室遇到X"，但故事写到第 45 章时角色自然走向了完全不同的方向，大纲就成了负担
3. **给 AI 自主权才有生命力** —— 在卷级目标和关键里程碑的约束下，具体路径留给小说家自主判断

### 推荐的大纲结构

```markdown
## 第一卷：卷名
- **卷级弧线**：主角从 X 状态到 Y 状态的核心变化
- **情感基调**：压抑→觉醒→首胜

### 必须里程碑（不绑定章节号）
1. 主角发现自己的特殊能力
2. 第一次实战
3. 遇到第一个重要盟友
4. 卷末：意识到更大的世界

### 方向提示（非强制，供参考）
- 某个次要情节线可在此卷引入
- 某个角色可在此卷初次登场
```

**关键原则**：只约束"必须发生什么"，不约束"在哪一章发生"。

---

## 十五、讨论日志结构

```json
{
  "sessions": [
    {
      "id": "disc_001",
      "timestamp": "2026-05-09T...",
      "trigger": "添加'元力共鸣'设定",
      "summary": "决定采用情感同步路线，限定在战斗搭档关系",
      "decisions": [
        {
          "type": "add_world_rule",
          "file": "世界观.md",
          "section": "元力共鸣",
          "detail": "两个魔型师心意相通时元力共振增幅"
        },
        {
          "type": "modify_character",
          "file": "角色.md",
          "character": "周瀚",
          "change": "新增：与林野的潜在共鸣搭档关系"
        },
        {
          "type": "modify_outline",
          "file": "大纲.md",
          "chapter_range": "第2卷",
          "change": "新增共鸣发现事件"
        }
      ],
      "applied": true
    }
  ]
}
```

---

## 十六、打回机制

### 打回时提供原因输入窗口

打回不是无脑点击，用户自己决定写什么原因：

```
生成章节展示后：
┌────────────────────────────────────────┐
│  第 42 章：重逢的铜丝                    │
│                                        │
│  [正文内容...]                          │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │  📝 打回原因（告诉小说家哪里不对）：  │  │
│  │                                  │  │
│  │  "这段对话太生硬了，苏棠的性格       │  │
│  │   不应该这么直接说出口，她应该是     │  │
│  │   拐弯抹角的那种。另外节奏太赶，     │  │
│  │   中间加一段内心独白缓冲一下"       │  │
│  └──────────────────────────────────┘  │
│                                        │
│  [✅ 接受]   [🔄 打回重写（带原因）]     │
│                                        │
│  如果只点打回不写原因 → 默认提示         │
│  "请重写，保持衔接但不满意当前版本"       │
└────────────────────────────────────────┘
```

打回原因直接注入到下一次写作的 human message 里作为 `rewrite_reason`。没有中间 Agent 传话——你亲自指出的问题，AI 直接针对性地改。

讨论模式下，确认执行修改前也有类似窗口，用户可以微调 AI 输出的修改方案。

---

## 十七、三接口配置

### 设计原则

三个独立可配置的 API 接口：

| 接口 | 用途 | 默认模型 | 默认温度 |
|------|------|---------|---------|
| **novelist** | 写作 + 讨论 | `deepseek-v4-pro` | 1.0 |
| **extractor** | 提取摘要/更新状态 | `deepseek-v4-flash` | 0.3 |
| **compressor** | 卷级压缩/日志压缩 | `deepseek-v4-flash` | 0.3 |

**写作和讨论共用 novelist 接口** —— persona 一致性很重要，用户会感知到"同一个小说家"的味道。

### config.py 接口配置

```python
# ═══════════════════════════════════════════════════
# 三接口配置（每个接口独立设置所有 DeepSeek API 参数）
# 参数名严格对应 DeepSeek API: https://api-docs.deepseek.com/api/create-chat-completion
# ═══════════════════════════════════════════════════

# --- 接口 1：小说家（写作 + 讨论）---
NOVELIST_CONFIG = {
    "model": os.getenv("NOVELIST_MODEL", "deepseek-v4-pro"),
    "temperature": float(os.getenv("NOVELIST_TEMPERATURE", "1.0")),
    "max_tokens": int(os.getenv("NOVELIST_MAX_TOKENS", "6000")),
    "top_p": float(os.getenv("NOVELIST_TOP_P", "1.0")),
    "frequency_penalty": float(os.getenv("NOVELIST_FREQUENCY_PENALTY", "0.3")),
    "presence_penalty": float(os.getenv("NOVELIST_PRESENCE_PENALTY", "0.2")),
    "enable_thinking": os.getenv("NOVELIST_ENABLE_THINKING", "false").lower() in ("true", "1", "yes"),
    "reasoning_effort": os.getenv("NOVELIST_REASONING_EFFORT", "medium"),
}

# --- 接口 2：提取器（章节摘要提取 + 状态更新）---
EXTRACTOR_CONFIG = {
    "model": os.getenv("EXTRACTOR_MODEL", "deepseek-v4-flash"),
    "temperature": float(os.getenv("EXTRACTOR_TEMPERATURE", "0.3")),
    "max_tokens": int(os.getenv("EXTRACTOR_MAX_TOKENS", "2000")),
    "top_p": float(os.getenv("EXTRACTOR_TOP_P", "1.0")),
    "frequency_penalty": float(os.getenv("EXTRACTOR_FREQUENCY_PENALTY", "0")),
    "presence_penalty": float(os.getenv("EXTRACTOR_PRESENCE_PENALTY", "0")),
    "enable_thinking": os.getenv("EXTRACTOR_ENABLE_THINKING", "false").lower() in ("true", "1", "yes"),
    "reasoning_effort": os.getenv("EXTRACTOR_REASONING_EFFORT", "low"),
    "response_format": {"type": "json_object"},  # 提取器强制 JSON 输出
}

# --- 接口 3：压缩器（卷级压缩 + 讨论日志压缩）---
COMPRESSOR_CONFIG = {
    "model": os.getenv("COMPRESSOR_MODEL", "deepseek-v4-flash"),
    "temperature": float(os.getenv("COMPRESSOR_TEMPERATURE", "0.3")),
    "max_tokens": int(os.getenv("COMPRESSOR_MAX_TOKENS", "2000")),
    "top_p": float(os.getenv("COMPRESSOR_TOP_P", "1.0")),
    "frequency_penalty": float(os.getenv("COMPRESSOR_FREQUENCY_PENALTY", "0")),
    "presence_penalty": float(os.getenv("COMPRESSOR_PRESENCE_PENALTY", "0")),
    "enable_thinking": os.getenv("COMPRESSOR_ENABLE_THINKING", "false").lower() in ("true", "1", "yes"),
    "reasoning_effort": os.getenv("COMPRESSOR_REASONING_EFFORT", "low"),
}
```

### 每个接口的参数说明（对应 DeepSeek API）

| 参数 | 类型 | 取值范围 | 说明 |
|------|------|---------|------|
| `model` | string | `deepseek-v4-flash` / `deepseek-v4-pro` | DeepSeek API 模型 ID（`deepseek-chat`/`deepseek-reasoner` 将弃用，分别对应 flash 非思考/思考模式） |
| `temperature` | float | 0 ~ 2 | 采样温度，越高越随机，越低越确定 |
| `max_tokens` | int | 1 ~ 模型上限 | 最大输出 token 数 |
| `top_p` | float | 0 ~ 1 | 核采样，只考虑概率质量前 top_p 的 token |
| `frequency_penalty` | float | -2 ~ 2 | 正值降低重复，负值增加重复 |
| `presence_penalty` | float | -2 ~ 2 | 正值鼓励新话题，负值鼓励维持话题 |
| `enable_thinking` | bool | true/false | DeepSeek 思考模式开关 |
| `reasoning_effort` | string | `low` / `medium` / `high` | 思考深度（仅 enable_thinking=true 时生效） |
| `response_format` | object | `{"type": "json_object"}` | 强制 JSON 输出（仅 extractor 使用） |

### 全局写作参数

```python
# --- 章节字数 ---
CHAPTER_WORD_COUNT_MIN = int(os.getenv("CHAPTER_WORD_COUNT_MIN", "2200"))
CHAPTER_WORD_COUNT_MAX = int(os.getenv("CHAPTER_WORD_COUNT_MAX", "2800"))

# --- 上下文约束 ---
CONTEXT_SOFT_LIMIT_TOKENS = int(os.getenv("CONTEXT_SOFT_LIMIT_TOKENS", "30000"))  # 超过此值触发二次压缩
RECENT_CHAPTERS_COUNT = int(os.getenv("RECENT_CHAPTERS_COUNT", "2"))              # 最近全文章节数
```

---

## 十八、知识文件初始化流程

### 最小启动信息

用户新开小说时只需提供：
1. **小说名**
2. **核心创意**（可以是一大段话，不必限制为一句话）
3. **主角名字 + 一句话人设**

### AI 引导式初始化

AI 在讨论模式下逐步引导用户填充知识文件：

```
用户："我想写一个在魔导编程世界里用代码战斗的少年"

AI：  "这个设定很有意思。让我先确认几个基本方向：
      1. 这个世界的力量体系是怎样的？魔力是天生的还是后天学习的？
      2. 主角的核心困境是什么？他为什么需要变强？
      3. 你心中这个故事的终点是什么样的？

      不用一次性回答，我们可以一个一个聊。"

→ 逐步填充 世界观.md / 角色.md / 大纲.md
→ 所有初始化都在对话中完成，不要求用户一次性填表
→ discussion_log 从第一次对话就开始记录
```

---

## 十九、错误恢复

### 原则

不过度设计。错误恢复的目标是"不丢数据 + 让用户知道发生了什么"，不追求自动修复一切。

### 四种失败模式

```
1. API 超时 / 500
   → 自动重试 2 次（间隔递增：3s → 6s）
   → 仍失败 → 提示用户，保留当前所有状态不变

2. 输出截断（finish_reason = "length"）
   → 自动续写 1 次（已写内容 + "请从断点继续完整写完"）
   → 仍截断 → 标记"未完成"给用户，用户决定接受现状还是打回

3. 提取模型 JSON 解析失败
   → 修复策略（补括号、清中文标点）→ 降级为文本提取
   → 再失败 → state.json 标记 "last_extraction_failed": true
   → 下一章写作时 human message 带警告："上一章状态提取失败，请注意衔接"

4. 讨论模式下知识文件写入失败
   → 总是在写入前创建快照（防丢失）
   → 先写临时文件（如 世界观.md.tmp），成功后再 rename 覆盖
   → rename 失败不破坏原有文件
```

---

## 二十、Web UI 交互设计

### 整体布局：双面板

```
┌──────────────────────┬──────────────────────────────┐
│  知识面板（左侧）      │  交互面板（右侧）              │
│                      │                              │
│  📖 世界观            │  ┌────────────────────────┐  │
│  👤 角色              │  │  对话流 / 章节预览      │  │
│  📋 大纲              │  │                        │  │
│  🎨 风格              │  │  类似 ChatGPT 的对话流   │  │
│  ⚙️ 状态              │  │  下半部分显示最近章节    │  │
│                      │  │                        │  │
│  [编辑] [讨论这个]    │  │  [输入框]               │  │
│                      │  │  [✍️ 写作] [💬 讨论] 切换│  │
└──────────────────────┴──────────────────────────────┘
```

### 两种模式的 UI 差异

- **写作模式**：对话流 + 章节预览 + "打回重写（带原因）"/"接受"按钮
- **讨论模式**：对话流 + 修改清单预览 + "确认执行修改"/"换个方案"按钮
- **回退界面**：时间线视图 + 两个按钮（"仅回退章节"/"回退一切"）

---

## 二十一、定价参考（DeepSeek API 官方）

按 DeepSeek API 官方文档实时价格，单位：¥/M tokens。

⚠️ 价格可能变动，以 [DeepSeek API Pricing](https://api-docs.deepseek.com/zh-cn/quick_start/pricing) 为准。

```python
# deepseek-v4-flash 和 deepseek-v4-pro 统一定价
PRICING = {
    "deepseek-v4-flash": {
        "input_cache_hit": 0.2,     # 缓存命中（元/M tokens）
        "input_cache_miss": 2.0,    # 缓存未命中
        "output": 3.0,              # 输出
    },
    "deepseek-v4-pro": {
        "input_cache_hit": 0.2,
        "input_cache_miss": 2.0,
        "output": 3.0,
    },
    # 兼容旧模型名（将被弃用，分别对应 flash 的非思考/思考模式）
    "deepseek-chat": {
        "input_cache_hit": 0.2,
        "input_cache_miss": 2.0,
        "output": 3.0,
    },
    "deepseek-reasoner": {
        "input_cache_hit": 0.2,
        "input_cache_miss": 2.0,
        "output": 3.0,
    },
}
```

---

## 二十二、模块职责与函数签名（API 契约）

> **本章是代码实现的直接规范**。每个 .py 文件的公开函数签名、返回值、职责边界都在这里。
> flash 写代码时必须严格按此契约实现，不得把函数放到错误的模块中。

### config.py

**职责**：环境变量 + 常量 + 三接口配置。无函数，只导出变量。

```python
# 导入方式：from config import NOVELIST_CONFIG, DEEPSEEK_API_KEY, ...
```

### token_tracker.py

**职责**：记录每次 API 调用的 token 用量和费用。

```python
class Tracker:
    def add_usage(self, agent_name: str, model: str,
                  input_tokens: int, output_tokens: int,
                  cache_hit_tokens: int, is_pro: bool) -> float:
        """追加一条调用记录，返回本次费用(元)"""
        ...

    def get_summary(self) -> dict:
        """返回汇总: {total_cost_yuan, call_count, total_cache_hit, total_input}"""
        ...
```

### state_manager.py

**职责**：所有 JSON 文件读写、状态持久化、快照、回退、摘要链、讨论日志。

```python
# --- 基础读写 ---
def load_state() -> dict:
    """加载 state.json，不存在则返回默认空状态"""

def save_state(state: dict) -> None:
    """保存 state.json"""

# --- 摘要链 ---
def load_summaries() -> list[dict]:
    """加载 summaries.json，返回 [{chapter: N, summary: "..."}]"""

def save_summaries(summaries: list[dict]) -> None:

def append_summary(chapter: int, summary: str) -> None:
    """追加一条摘要（同时自动触发压缩检查）"""

def get_summary_text(current_volume_only: bool = True) -> str:
    """
    获取摘要文本，用于 system prompt。
    current_volume_only=True → 只返回当前卷逐章摘要
    current_volume_only=False → 返回当前卷摘要 + 旧卷摘要
    """

def compress_to_volume_summary() -> str:
    """将当前卷所有摘要压缩为一卷级摘要（调用 compressor LLM），返回卷摘要文本"""

# --- 讨论日志 ---
def load_discussion_log() -> dict:
    """加载 discussion_log.json"""

def save_discussion_log(log: dict) -> None:

def get_discussion_summary(max_tokens: int = 500) -> str:
    """提取讨论日志摘要文本，用于 system prompt。不超 max_tokens 字"""

# --- 快照 ---
def save_snapshot(chapter_num: int) -> None:
    """保存当前 state + summaries 为快照 ch_XXX.json"""

def list_snapshots() -> list[int]:
    """返回所有快照章节号列表（降序）"""

def rollback_to_chapter(chapter_num: int, rollback_discussions: bool = False) -> dict:
    """
    回退到指定章节快照。
    rollback_discussions=True → 同时恢复讨论前的知识文件
    返回恢复后的 state
    """

def save_discussion_snapshot_pre(disc_id: str, knowledge_files: list[str]) -> None:
    """讨论执行前：备份指定知识文件的副本到 disc_XXX_pre.json"""

def save_discussion_snapshot_post(disc_id: str) -> None:
    """讨论执行后：保存当前状态到 disc_XXX_post.json"""

# --- 事件时间线 ---
def record_event(event_type: str, data: dict) -> None:
    """
    追加事件到 events.jsonl。
    event_type: "chapter" / "discussion" / "rollback"
    data: {"chapter": N} 或 {"disc_id": "...", "summary": "..."} 等
    """

def get_timeline_events(since_chapter: int = 0) -> list[dict]:
    """返回 since_chapter 之后的所有事件（用于回退分析）"""

# --- 知识文件读写 ---
def read_knowledge_file(filename: str) -> str:
    """读取 knowledge/xxx.md，不存在返回空字符串"""

def write_knowledge_file(filename: str, content: str) -> None:
    """原子写入：先写 .tmp，再 rename"""

# --- 输出章节 ---
def save_output_chapter(chapter_num: int, chapter_text: str) -> str:
    """保存章节到 output/第XXX章_标题.md，返回文件路径"""

def delete_output_chapters_after(chapter_num: int) -> None:
    """删除所有 > chapter_num 的输出章节文件"""
```

### system_builder.py

**职责**：根据知识文件 + 状态 + 讨论日志 → 组装成不同的 system prompt。

```python
def build_writer_prompt(state: dict) -> str:
    """
    组装写作模式 system prompt。
    结构：persona → 世界观 → 角色 → 风格 → 大纲 → 讨论摘要 → 旧卷摘要 → 章节摘要
    返回 system message 的完整字符串（不含 human message）
    """

def build_discussion_prompt(state: dict) -> str:
    """
    组装讨论模式 system prompt。
    Persona 是"创意合伙人"，加载所有知识文件 + 讨论历史。
    返回 system message 的完整字符串
    """

def build_extractor_prompt(chapter_text: str, state: dict) -> tuple[str, str]:
    """
    组装提取器 prompt。
    返回 (system_message, human_message)
    """

def build_compressor_prompt(summaries_text: str) -> tuple[str, str]:
    """
    组装压缩器 prompt。
    返回 (system_message, human_message)
    """

def estimate_tokens(text: str) -> int:
    """估算中文文本的 token 数（≈ 字数 × 1.5）"""
```

### novelist.py

**职责**：核心业务逻辑。写作、讨论、接受章节、执行修改。web_ui 只直接调用此模块。

```python
def generate_chapter(user_direction: str = "",
                     is_rewrite: bool = False,
                     rewrite_reason: str = "") -> dict:
    """
    生成一章正文（写作模式）。

    内部流程：
    1. state_manager.load_state()
    2. system_builder.build_writer_prompt(state)
    3. 调用 LLM（novelist 接口）
    4. 解析正文 + Agent 附言 + 卷切换信号

    返回：
    {
        "chapter_text": str,       # 纯正文
        "agent_suggestion": str,   # Agent 附言
        "usage_info": dict,        # token 用量
        "volume_switch": dict | None,  # {"num": N, "title": "..."} 或 None
        "chapter_num": int,
        "conflict_alert": str | None,  # 如果小说家主动提示矛盾
    }
    """

def accept_chapter(chapter_text: str,
                   agent_suggestion: str,
                   usage_info: dict,
                   volume_switch: dict | None) -> dict:
    """
    接受章节，执行后处理。

    内部流程：
    1. 调用 LLM（extractor 接口）提取章节摘要 + 更新状态
    2. state_manager.save_snapshot(chapter_num)
    3. state_manager.append_summary(chapter, summary)
    4. 如果 volume_switch → 触发卷级压缩
    5. state_manager.save_state(updated_state)
    6. state_manager.save_output_chapter(chapter_num, chapter_text)
    7. state_manager.record_event("chapter", ...)

    返回：{"success": bool, "message": str, "next_chapter": int}
    """

def discuss(user_message: str) -> dict:
    """
    讨论模式：用户提出想法，小说家分析并给出方案。

    内部流程：
    1. state_manager.load_state()
    2. system_builder.build_discussion_prompt(state)
    3. 调用 LLM（novelist 接口）
    4. 解析回复 + 修改清单

    返回：
    {
        "reply": str,                    # 小说家的分析回复
        "modification_plan": {           # 修改清单（可多轮迭代）
            "analyses": [
                {"aspect": "世界观", "impact": "...", "risk": "..."},
                {"aspect": "角色", "impact": "...", "risk": "..."},
            ],
            "options": [                 # 2-3 个方案
                {
                    "label": "方案名",
                    "description": "...",
                    "files_to_modify": [
                        {"file": "世界观.md", "changes": "具体修改内容"}
                    ]
                }
            ],
            "recommendation": "推荐哪个方案及理由"
        }
    }
    """

def execute_modifications(modifications: list[dict]) -> dict:
    """
    执行讨论确认后的知识文件修改。

    modifications 结构：
    [
        {"file": "世界观.md", "action": "append_section", "section_title": "...", "content": "..."},
        {"file": "角色.md", "action": "update_character", "character": "周瀚", "field": "...", "value": "..."},
        {"file": "大纲.md", "action": "insert_milestone", "volume": 2, "content": "..."},
    ]

    内部流程：
    1. 生成 disc_id
    2. state_manager.save_discussion_snapshot_pre(disc_id, affected_files)
    3. 逐文件执行修改（state_manager.write_knowledge_file）
    4. state_manager.save_discussion_snapshot_post(disc_id)
    5. state_manager.save_discussion_log()
    6. state_manager.record_event("discussion", ...)

    返回：{"success": bool, "message": str, "disc_id": str}
    """

def init_novel(name: str, idea: str, protagonist: str) -> dict:
    """
    初始化一本新小说。

    内部流程：
    1. 创建 knowledge/ 目录结构
    2. 写入空白知识文件模板
    3. 初始化 state.json（chapter=1, volume=1）
    4. 开始第一次讨论对话，引导用户填充知识文件

    返回：{"success": bool, "message": str, "state": dict}
    """
```

### web_ui.py

**职责**：Gradio 界面。纯 UI 逻辑，不包含任何业务逻辑。所有业务逻辑通过调用 novelist.py 和 state_manager.py 完成。

```python
# UI 直接调用的函数（参考 V2 模式）：
# - novelist.generate_chapter()
# - novelist.accept_chapter()
# - novelist.discuss()
# - novelist.execute_modifications()
# - novelist.init_novel()
# - state_manager.load_state()
# - state_manager.list_snapshots()
# - state_manager.rollback_to_chapter()
# - state_manager.get_timeline_events()
# - token_tracker.get_summary()
```

---

## 二十三、数据流（精确调用序列）

> **以下序列是 flash 写代码时的执行路线图。每一步的输入输出都已指定。**

### 流程 1：生成章节（写作模式）

```
web_ui.on_generate(user_direction, is_rewrite, rewrite_reason)
  │
  ├─ ① state = state_manager.load_state()
  │     返回: {next_chapter, current_volume, current_scene, characters, foreshadowing, suspense, ...}
  │
  └─ ② result = novelist.generate_chapter(user_direction, is_rewrite, rewrite_reason)
          │
          ├─ 内部: system = system_builder.build_writer_prompt(state)
          │        顺序: persona → 世界观.md → 角色.md → 风格.md → 续写规范.md 
          │              → 大纲.md → 讨论摘要 → 旧卷摘要 → 当前卷章节摘要 
          │              （全部写入 system message）
          │
          ├─ 内部: human = f"写第{ch}章。{user_direction}\n\n"
          │              f"时空：{state.current_scene}, {state.current_time}\n"
          │              f"角色位置：{...}\n"
          │              f"最近2章：{...}\n"
          │              f"{rewrite_reason_section}"
          │
          ├─ 内部: llm_response = llm.invoke([SystemMessage(system), HumanMessage(human)])
          │
          ├─ 内部: 解析正文、Agent附言、卷切换信号
          │
          └─ 返回: {chapter_text, agent_suggestion, usage_info, volume_switch, chapter_num, conflict_alert}
```

### 流程 2：接受章节

```
web_ui.on_accept()
  │
  └─ ① result = novelist.accept_chapter(chapter_text, agent_suggestion, usage_info, volume_switch)
          │
          ├─ 内部: system, human = system_builder.build_extractor_prompt(chapter_text[:6000], state)
          │
          ├─ 内部: extract_result = extractor_llm.invoke(...)  # JSON 输出
          │        提取: summary(150字), location, characters(位置+状态),
          │              foreshadowing(新增+回收), suspense, timeline_events
          │
          ├─ 内部: state_manager.save_snapshot(chapter_num)
          │
          ├─ 内部: state_manager.append_summary(chapter_num, summary)
          │
          ├─ 内部: if volume_switch:
          │            state_manager.compress_to_volume_summary()
          │            state["current_volume"] = volume_switch
          │
          ├─ 内部: state["next_chapter"] += 1
          │        state["current_scene"] = extracted_scene
          │        state["current_suspense"] = extracted_suspense
          │        更新 characters、foreshadowing、suspense、timeline_events
          │        state_manager.save_state(state)
          │
          ├─ 内部: state_manager.save_output_chapter(chapter_num, chapter_text)
          │
          ├─ 内部: state_manager.record_event("chapter", {"chapter": chapter_num})
          │
          └─ 返回: {success: True, message: "...", next_chapter: N}
```

### 流程 3：讨论模式

```
web_ui.on_discuss(user_message)
  │
  └─ ① result = novelist.discuss(user_message)
          │
          ├─ 内部: state = state_manager.load_state()
          │
          ├─ 内部: system = system_builder.build_discussion_prompt(state)
          │        Persona: "创意合伙人"
          │        含: 全部知识文件 + 大纲 + 讨论历史 + 当前进度
          │
          ├─ 内部: human = f"作者的想法：{user_message}\n请分析影响并给出方案。"
          │
          ├─ 内部: llm_response = novelist_llm.invoke(...)
          │
          ├─ 内部: 解析 分析 + 方案 + 修改清单
          │
          └─ 返回: {reply, modification_plan}

web_ui.on_confirm_modifications(modification_plan.selected_option.files_to_modify)
  │
  └─ ② result = novelist.execute_modifications(modifications)
          │
          ├─ 内部: disc_id = f"disc_{int(time.time())}"
          │        affected_files = [m["file"] for m in modifications]
          │        state_manager.save_discussion_snapshot_pre(disc_id, affected_files)
          │
          ├─ 内部: for mod in modifications:
          │            content = state_manager.read_knowledge_file(mod["file"])
          │            new_content = apply_change(content, mod)
          │            state_manager.write_knowledge_file(mod["file"], new_content)
          │            # write_knowledge_file 内部: 写 .tmp → rename
          │
          ├─ 内部: state_manager.save_discussion_snapshot_post(disc_id)
          │
          ├─ 内部: log = state_manager.load_discussion_log()
          │        log["sessions"].append({id: disc_id, trigger: "...", decisions: modifications})
          │        state_manager.save_discussion_log(log)
          │
          ├─ 内部: state_manager.record_event("discussion", {disc_id, summary, files})
          │
          └─ 返回: {success: True, message: "...", disc_id}
```

### 流程 4：回退

```
web_ui.on_rollback(target_chapter, rollback_discussions)
  │
  ├─ ① events = state_manager.get_timeline_events(since_chapter=target_chapter)
  │     返回: [{"type": "discussion", "disc_id": "...", ...}, {"type": "chapter", "chapter": 50}]
  │
  ├─ ② 展示影响范围（章节 + 讨论），用户确认
  │
  └─ ③ state = state_manager.rollback_to_chapter(target_chapter, rollback_discussions)
          │
          ├─ 内部: 从 ch_XXX.json 恢复 state + summaries
          ├─ 内部: if rollback_discussions and disc_snapshot_exists:
          │            从 disc_XXX_pre.json 恢复知识文件
          ├─ 内部: state_manager.delete_output_chapters_after(target_chapter)
          ├─ 内部: 清理 > target_chapter 的快照文件
          ├─ 内部: state_manager.record_event("rollback", {target_chapter, rollback_discussions})
          │
          └─ 返回: 恢复后的 state
```

---

## 二十四、实现顺序

> **严格的编写顺序。前一步完成并验证后才能开始下一步。**
> 每一阶段都列出了该阶段完成后可以独立测试的方式。

### 阶段 0：项目骨架

| 顺序 | 文件 | 内容 | 完成后如何验证 |
|------|------|------|--------------|
| 0.1 | `requirements.txt` | 依赖清单 | `pip install -r requirements.txt` |
| 0.2 | `.env.example` | 环境变量模板 | 复制为 `.env` 并填入 API Key |
| 0.3 | `config.py` | 三接口配置 + 常量 + 路径 | `python -c "import config; print(config.NOVELIST_CONFIG)"` |

### 阶段 1：基础设施（无 LLM 依赖）

| 顺序 | 文件 | 内容 | 完成后如何验证 |
|------|------|------|--------------|
| 1.1 | `token_tracker.py` | Tracker 类 | `pytest tests/test_tracker.py` |
| 1.2 | `state_manager.py` | 所有 JSON 读写函数 | `pytest tests/test_state_manager.py` |
| 1.3 | `knowledge/` 目录 | 创建空的知识文件模板 | 确认目录和文件存在 |
| 1.4 | `state_manager.py` | 补充压缩函数（依赖 compressor LLM） | 此步可先跳过，阶段 3 补全 |

### 阶段 2：核心引擎（依赖阶段 1）

| 顺序 | 文件 | 内容 | 完成后如何验证 |
|------|------|------|--------------|
| 2.1 | `system_builder.py` | `build_writer_prompt` + `build_discussion_prompt` + `estimate_tokens` | 用假 state 调用，检查输出结构 |
| 2.2 | `novelist.py` | `generate_chapter`（写作模式） | 调用一次，检查能否写出章节 |
| 2.3 | `system_builder.py` | `build_extractor_prompt` | |
| 2.4 | `novelist.py` | `accept_chapter`（提取 + 更新状态） | 接受一章，检查 state + summaries 更新 |

### 阶段 3：讨论引擎（依赖阶段 2）

| 顺序 | 文件 | 内容 | 完成后如何验证 |
|------|------|------|--------------|
| 3.1 | `novelist.py` | `discuss`（讨论模式） | 发起讨论，检查分析 + 方案输出 |
| 3.2 | `novelist.py` | `execute_modifications` | 确认修改后检查知识文件变化 |
| 3.3 | `state_manager.py` | 补全压缩函数（compress_to_volume_summary） | |
| 3.4 | `system_builder.py` | `build_compressor_prompt` | |

### 阶段 4：小说初始化（依赖阶段 3）

| 顺序 | 文件 | 内容 | 完成后如何验证 |
|------|------|------|--------------|
| 4.1 | `novelist.py` | `init_novel` | 初始化一本新小说，检查知识文件 + state |

### 阶段 5：Web UI（依赖阶段 4）

| 顺序 | 文件 | 内容 | 完成后如何验证 |
|------|------|------|--------------|
| 5.1 | `web_ui.py` | 基础布局：双面板 | `python web_ui.py` 打开 localhost |
| 5.2 | `web_ui.py` | 写作模式：生成 + 接受 + 打回 | 完整走一遍写→接受流程 |
| 5.3 | `web_ui.py` | 讨论模式：对话 + 确认修改 | 完整走一遍讨论→确认流程 |
| 5.4 | `web_ui.py` | 回退界面：时间线 + 两种回退 | 测试回退 |
| 5.5 | `web_ui.py` | 初始化向导 | 测试从零开始 |

### 依赖关系速查

```
config.py ← (无依赖，最先写)
token_tracker.py ← (无依赖)
state_manager.py ← config
  └─ system_builder.py ← state_manager, config
       └─ novelist.py ← system_builder, state_manager, token_tracker, config
            └─ web_ui.py ← novelist, state_manager, token_tracker, config
```

---

## 二十五、构建 LLM 工具函数

> **三个接口的 LLM 构建函数，避免重复代码。位于 novelist.py 顶部。**

```python
def _build_llm(config_dict: dict) -> ChatOpenAI:
    """
    根据接口配置字典构建 ChatOpenAI 实例。

    config_dict 结构见十七章三接口配置。
    自动处理 thinking 模式参数注入。

    参数映射（config → DeepSeek API）：
      model        → model
      temperature  → temperature
      max_tokens   → max_tokens
      top_p        → model_kwargs["top_p"]
      frequency_penalty → model_kwargs["frequency_penalty"]
      presence_penalty  → model_kwargs["presence_penalty"]
      enable_thinking   → model_kwargs["extra_body"]["thinking"]["type"]
      reasoning_effort  → model_kwargs["extra_body"]["thinking"]["reasoning_effort"]
      response_format   → model_kwargs["response_format"]

    注意：enable_thinking=True 时，temperature 和 top_p 必须移除（DeepSeek API 要求）
    """
    ...

# 模块级单例（复用 HTTP 连接，最大化缓存命中）
_novelist_llm = None
_extractor_llm = None
_compressor_llm = None

def _get_novelist_llm() -> ChatOpenAI: ...
def _get_extractor_llm() -> ChatOpenAI: ...
def _get_compressor_llm() -> ChatOpenAI: ...
```

---

## 二十六、可扩展性设计

### 当前假设与扩展预留

当前项目假设"单小说"，目录结构硬编码在项目根目录。以下扩展点需要预留接口，后期不重构。

### 多小说支持

改动最小方案：小说名作为工作空间键。

```
NovelNexus_V3/
├── novels/
│   ├── 元构魔能/
│   │   ├── knowledge/
│   │   │   ├── 世界观.md
│   │   │   ├── 角色.md
│   │   │   ├── 大纲.md
│   │   │   ├── 风格.md
│   │   │   ├── 续写规范.md
│   │   │   ├── state.json
│   │   │   ├── summaries.json
│   │   │   ├── discussion_log.json
│   │   │   ├── style_progression.json    # 🆕 风格演化记录
│   │   │   └── snapshots/
│   │   └── output/
│   └── 另一本小说/
│       └── ...
├── config.py          # 全局配置（含当前活跃小说名）
├── novelist.py        # 接受 novel_name 参数
├── system_builder.py
├── state_manager.py   # 接受 novel_name 参数
├── web_ui.py
└── token_tracker.py   # 跨小说累计
```

`config.py` 增加：

```python
ACTIVE_NOVEL = os.getenv("NOVELNEXUS_ACTIVE_NOVEL", "default")
NOVELS_DIR = os.path.join(BASE_DIR, "novels")

def get_novel_dir(novel_name: str = None) -> str:
    """获取当前小说的根目录"""
    name = novel_name or ACTIVE_NOVEL
    return os.path.join(NOVELS_DIR, name)

def get_novel_knowledge_dir(novel_name: str = None) -> str:
    return os.path.join(get_novel_dir(novel_name), "knowledge")

def get_novel_output_dir(novel_name: str = None) -> str:
    return os.path.join(get_novel_dir(novel_name), "output")
```

`state_manager.py` 和 `novelist.py` 的所有函数签名不变（内部通过 `config.get_novel_*()` 定位路径），通过 `ACTIVE_NOVEL` 环境变量切换。

**当前阶段不实现多小说 UI 切换**，但目录结构和所有路径函数预留此扩展。

### 多 LLM Provider 支持

`novelist.py` 的 `_build_llm` 已经接受 `config_dict`，通过更改模型名即可切换 provider。不做额外抽象层。

### Hook 机制（预留，非必须）

```python
# novelist.py 内部预留 hook 调用点
# 当前为空实现，后续可注入自定义逻辑

_HOOKS = {
    "before_generate": [],   # 章节生成前
    "after_accept": [],      # 接受章节后
    "before_discuss": [],    # 讨论前
    "after_modify": [],      # 知识文件修改后
}

def register_hook(event: str, callback: callable):
    _HOOKS[event].append(callback)
```

不实现，只留接口。

---

## 二十七、模拟真人作者的深层元认知

> **核心认知**：Prompt 工程有不可逾越的天花板。真人作者的"神经元固化"（风格本能、角色直觉、情感通感、节奏呼吸、审美 taste）无法通过塞文字来获得。
>
> 策略不是"弥补"，而是：**让 DeepSeek 用超人的维度撑起质量底线，把本能的维度留给用户的 taste 把关，同时设计"显式知识→近似本能"的蒸馏机制。**

### 真人作者 vs Prompt 工程 vs DeepSeek 超能力

| 能力维度 | 真人作者（神经元固化） | Prompt 工程（V3） | DeepSeek 1M 超能力 |
|---------|---------------------|-------------------|-------------------|
| 风格本能 | 300章后手指自会写，不需想 | 每章重读风格.md，如第一次写 | N/A |
| 角色直觉 | 角色"活"在脑中，不需查设定 | 每章重读角色.md | N/A |
| 情感通感 | 写到对味自己会起鸡皮疙瘩 | 可分析情绪弧线，无法"感受" | N/A |
| 节奏呼吸 | 自然知道快慢，像呼吸 | 需显式规则，易机械执行 | N/A |
| 审美taste | 数千小时阅读沉淀 | 无，只有模式匹配 | N/A |
| ═══════ | ═══════════════════ | ═══════════════════ | ═══════════════════ |
| 细节记忆 | 忘了第47章配角眼睛颜色 | 没摘要→忘；有全文→记 | **🟢 完美，绝不搞错** |
| 多线追踪 | 同时活跃3-5条线 | 受注意力布局限制 | **🟢 可追踪数十条线** |
| 跨章模式 | 几乎注意不到意象重复 | 除非刻意搜索 | **🟢 天然模式识别** |
| 时间精度 | 模糊的"大概三天前" | 依赖timeline质量 | **🟢 输入精确=输出精确** |
| 规模一致性 | 300章会疲劳、矛盾 | 受上下文管理限制 | **🟢 不疲劳** |

### 关键洞察

**上面 5 行是 prompt 工程的硬天花板**——不可能突破。**下面 5 行是 DeepSeek 原生超人的区域**——真人作者永远做不到。

V3 的策略：上面 5 行靠用户 taste（打回重写）把关；下面 5 行靠架构释放 DeepSeek 潜力来做到极致使 taste 把关更少。

---

### 维度 1：风格巩固层（Style Consolidation）

**问题**：每章重读 `风格.md`，但你的真实写作风格会在 50 章后发生微妙变化——某些句式反复用、某些留白习惯自然形成——这些都不在最初写的风格.md 里。小说家永远"像第 1 次写"。

**方案**：每写完一卷（约 50 章），触发风格萃取：

```
本卷全部章节原文 → compressor LLM →
提取：
  · 高频句式结构（短句打头→长句展开→短句收尾）
  · 对话占比趋势（早期25% → 现在35%）
  · 无意识偏好的意象（"铅灰色"出现23次）
  · 段落长度分布变化
  · 叙述者距离变化（更近了/更远了）

→ 写入 knowledge/style_progression.json
→ 下卷写作时，在风格.md后追加：
  "你最近一卷实际形成的风格特征：..."
```

**效果**：小说家会有"我已经写了 50 章，手感在那"的近似本能。虽然本质还是文字，但它是从实际产出蒸馏的，不是初始设定。

### 维度 2：角色行为内核（Character Behavior Kernel）

**问题**：角色在系统里只是文字描述。"林野不会这样做"的判断来自数百个已写场景的积累——相似情境中的反应模式已经直觉化。

**方案**：`state.json` 每个角色增加 `behavior_kernel` 字段，由提取器增量更新：

```json
"林野": {
  "location": "...",
  "state": "...",
  "behavior_kernel": {
    "under_threat": "沉默→观察→极端冷静的暴力",
    "with_strangers": "三句内暴露不善社交，用术语掩饰",
    "when_wrong": "不辩解，用行动证明",
    "emotional_tells": "右手指尖敲桌面=紧张；眼神不躲却沉默=愤怒",
    "dialogue_signature": "短句，爱反问，生气时话更短",
    "decision_pattern": "永远选最危险但最利他的路",
    "last_updated_chapter": 152
  }
}
```

每次 `accept_chapter` 时提取器检查角色行为是否呈现新模式，增量更新 `behavior_kernel`。

写作时 `behavior_kernel` 被放在 human message 高注意力区（紧挨 state 快照），让小说家写角色每句话时能"感觉到"这个人的底色。

**关键差异**：`behavior_kernel` 是从已写章节蒸馏的行为数据，不是初始设定。模拟的是"写了 200 章后对角色的本能理解"。

### 维度 3：小说家自我进化 —— 卷后回顾

**问题**：第 10 卷和第 1 卷的小说家拿到的是完全相同的 system prompt。真人作者写完一卷后会自然反思"这卷哪些地方写好了，哪些写飘了"。

**方案**：每卷结束（卷切换确认后），自动触发一次卷后回顾：

```
小说家（讨论模式）收到：
  "第N卷已结束。请回顾本卷全部章节正文，分析：
   1. 你做得最好的3个场景——它们好在哪里？
   2. 写得最勉强的3个场景——问题出在哪？
   3. 下一卷你自己要注意什么？

→ 输出 ≤ 800字「自我校准笔记」
→ 存入 state.json 的 self_calibrations 数组
→ 下一卷写作时注入 system prompt 中段：
  "上一卷你自己的回顾：..."
```

这不是 taste 替代（taste 仍由你把关），但它让小说家有"成长感"——积累了 9 份校准笔记的小说家，和初始的小说家是不同的。

### 维度 4：选择性遗忘（Forgetting as a Feature）

**问题**：真人作者的"遗忘"很多时候是好事——不重要的细节自然沉没。但系统把所有东西都存下来，有"过度一致"的风险（第 17 章路人甲的随口一句话，被第 200 章的 AI 当成重要伏笔郑重回收）。

**方案**：`state.json` 的伏笔/悬念增加 `importance` 字段（1-5）：

```
importance 5：主角核心谜团        → 每章注入
importance 4：当前卷关键冲突      → 每章注入
importance 3：支线伏笔            → 相关场景时注入
importance 2：路人细节            → 3章未引用→自动沉没（不注入，但可查）
importance 1：一次性背景          → 写完即沉没
```

自动降级规则：
- 伏笔超过 30 章未提及且 `importance ≤ 2` → 自动降为 `importance 1`
- 悬念超过 20 章未推进且 `importance ≤ 2` → 自动降为 `importance 1`
- 降级不是删除，只是不再注入上下文

**效果**：模拟真人作者"不重要的自然淡出"。

### 维度 5：释放 DeepSeek 超能力

**全本健康检查**（每 50 章自动触发一次）：

```
所有摘要链 + state.json → compressor LLM →
输出结构化诊断报告：
  · 设定违规：如"第47章林野左撇子，第152章写成右手"
  · 长眠伏笔：超 100 章未回收且 importance ≥ 3 的伏笔
  · 角色失踪：超 50 章未出场的活跃角色
  · 意象膨胀：同一意象出现次数超过阈值的警告
  · 伏笔提醒：importance ≥ 3 且超 30 章未推进的伏笔 → 
    自动生成 prompt 片段："以下伏笔久未推进，如有合适机会请呼应：..."
```

诊断结果存入 `state.json` 的 `health_report` 字段。后续写作时 `system_builder` 自动从中提取伏笔提醒注入 human message。

这是人类作者做不到的——你没有精力扫 300 章找呼应机会和矛盾。DeepSeek 1M 上下文可以。

---

## 二十八、V3 完整能力对照

| 维度 | V1 | V2 | V3 |
|------|-----|-----|-----|
| Agent 架构 | 5 Agent 管道 | 1 Agent | 1 Agent 双模式（写作/讨论） |
| 时空完整度 | ❌ 碎片化 | ⚠️ 基本可用 | ✅ 闭环真相源 |
| 记忆模型 | ❌ 无 | ⚠️ 摘要链 | ✅ 金字塔压缩（全文/摘要/卷级） |
| 缓存优化 | ❌ 无 | ✅ 分层缓存 | ✅ 细分层 + 三条铁律 |
| 讨论能力 | ❌ | ❌ | ✅ 合伙人模式 |
| 卷切换 | ❌ | ⚠️ 手动 | ✅ 自主提议 |
| 回退 | ❌ | ✅ 章节 | ✅ 章节+讨论双层快照 |
| 风格本能近似 | ❌ | ❌ | ✅ 风格巩固层（卷级蒸馏 + 手感摘要） |
| 角色直觉近似 | ❌ | ❌ | ✅ behavior_kernel（从章节行为蒸馏） |
| 自我进化 | ❌ | ❌ | ✅ 卷后回顾 + 叙事节拍自检 |
| 场景经验 | ❌ | ❌ | ✅ 场景类型目录（复用率 + 风险提醒） |
| 选择性遗忘 | ❌ | ❌ | ✅ importance 降级机制 |
| 读者疲劳感知 | ❌ | ❌ | ✅ 叙事节拍检测（高张力/同类型连续警告） |
| DeepSeek 超能力 | ❌ | ❌ | ✅ 全本健康检查（设定违规/长眠伏笔/意象膨胀） |
| 写作微调 | ❌ | ❌ | ✅ 🆕 四维微调（节奏/对话比例/动作比例/环境描写） |
| 知识快速编辑 | ❌ | ❌ | ✅ 知识面板直接编辑 Markdown |
| 多小说支持 | ❌ | ❌ | ✅ novels/ 命名空间 |
| 跨小说成本 | ❌ | ❌ | ✅ 按小说拆分 token 成本 |
| 可扩展性 | ❌ | ❌ | ✅ 多 Provider + Hook 预留 |

---

## 二十九、耦合度验证（多小说切换分析）

### 新开小说完整操作路径

```
操作：web_ui 点"新小说"→ 填名字 + 创意 + 主角
  ↓
① novelist.init_novel("小说名", "创意", "主角")
   → config.get_novel_dir("小说名")
   → 创建 novels/小说名/knowledge/ + output/
   → 写入空知识文件模板 + 初始化 state.json
   → 调用 novelist.discuss() 开始第5轮引导对话
  ↓
② config.set_active_novel("小说名")  ← 运行时切换
  ↓
③ web_ui 刷新 → 后续操作默认指向该小说
```

### 各模块切换成本

| 模块 | 是否感知小说名 | 耦合方式 | 切换新小说需要改动吗 |
|------|-------------|---------|-------------------|
| `config.py` | ✅ 存储 `ACTIVE_NOVEL` | 唯一耦合点 | 加 `set_active_novel()` + `list_novels()` |
| `state_manager.py` | ❌ 通过 `config.get_novel_*()` | 零耦合 | 不改 |
| `novelist.py` | ❌ 同上 | 零耦合 | 不改 |
| `system_builder.py` | ❌ 通过 state_manager 读知识文件 | 零耦合 | 不改 |
| `token_tracker.py` | ❌ 全局累计 | 零耦合 | 小改：加可选 `novel_name` 参数 |
| `web_ui.py` | ✅ 需小说选择器 | UI 层 | 加下拉菜单 |

**结论：耦合度很低。** 核心模块（novelist / state_manager / system_builder / token_tracker）不需要改业务逻辑。只有 `config.py` 需要加两个辅助函数，`web_ui.py` 需要加一个下拉菜单。

### config.py 补充函数

```python
def set_active_novel(name: str) -> None:
    """运行时切换活跃小说"""
    global ACTIVE_NOVEL
    ACTIVE_NOVEL = name

def list_novels() -> list[str]:
    """枚举 novels/ 下所有小说目录名"""
    if not os.path.isdir(NOVELS_DIR):
        return []
    return [d for d in os.listdir(NOVELS_DIR)
            if os.path.isdir(os.path.join(NOVELS_DIR, d)) and not d.startswith(".")]
```

---

## 三十、补充维度

> **上一轮冗余分析后保留的维度。灵感发散模式和初始化引导采访已确认冗余并删除（讨论模式自然覆盖）。**

---

### 维度 1：近期手感摘要（Recent Texture）

**缺口**：真人作者坐下来写第 200 章前会下意识感知自己最近几章的写感——对话多了还是少了、段落变长还是变短了。我们缺这个"自我感知"。

**方案**：每次写作前，`system_builder` 自动分析最近 3 章已写正文，生成手感摘要，注入 human message（建议区，非硬约束）：

```
"你最近 3 章的写作趋向：
 · 对话占比：30% → 35% → 38%（上升中，本章可保持或回调）
 · 平均段落长度：4.2句 → 3.8句 → 3.5句（变短中，节奏加快）
 · 感官覆盖：3 → 4 → 4 种
 · 近期核心意象：'铜丝''铅灰云层''掌心的茧'
 · 近期场景类型：对话70% / 内心独白20% / 动作10%
   （场景类型略偏单一，本周可考虑加入更多动作或环境描写）
→ 这是你的自然手感，不是强制约束。但如果偏离太远，说明你可能在偏离自己。"
```

**这不是规则，是自我感知。** 真人作者的手感就是这个作用——知道最近的节奏，也知道什么时候该打破它。

**与风格巩固层的关系**：手感摘要是风格巩固层的即时消费端——读取同一数据源（每章的 `emotional_arc` + `used_expressions`），但不需要 LLM 处理，纯计算即可。卷级总结用 compressor LLM，手感摘要用统计函数。一个数据源，两个消费端。

---

### 维度 2：叙事节拍检测（Rhythm Fatigue Warning）

**缺口**：真人作者有本能感知"连续高潮读者会累"。AI 没有本能，但可以有数据。

**方案**：`system_builder.build_writer_prompt()` 中自动检查最近章节的 tension 值和内容类型，在 human message 中给出提醒：

```
写作前自动检查：
  最近 5 章 tension：7, 8, 9, 8, 9
  → "⚠️ 连续 5 章高张力（≥7），读者可能疲劳。
     建议本章安排低张力过渡场景（tension 3-5），
     如日常对话、内心独白、环境描写为主的喘息章节。"

  最近 3 章内容类型：战斗, 战斗, 战斗
  → "⚠️ 连续 3 章同类型场景，节奏单一。
     建议本章切换场景类型（对话/探索/日常/内心）。"
```

阈值可配置：

```python
RHYTHM_CONFIG = {
    "max_consecutive_high_tension": 4,   # 连续高张力章数上限
    "high_tension_threshold": 7,          # 高张力阈值（1-10）
    "max_consecutive_same_scene_type": 3, # 连续同类型场景上限
}
```

---

### 维度 3：场景类型目录（Scene Catalog）

**缺口**：真人作者写久了会自然知道这本小说里哪种场景屡试不爽。这不是从设定来的，是写出来的经验。

**方案**：`state.json` 增加 `scene_catalog` 字段，由提取器在 `accept_chapter` 时增量更新：

```json
"scene_catalog": [
  {
    "type": "夜间屋顶对话",
    "characters": ["林野", "苏棠"],
    "appearances": [12, 34, 67, 89],
    "effectiveness": "高 — 每次推动角色关系关键转折",
    "signature_pattern": "远景星空→近景两人背影→沉默→一句打破沉默的话",
    "risk": "频率正常（4次/89章）"
  },
  {
    "type": "实战中的顿悟",
    "characters": ["林野"],
    "appearances": [8, 15, 23, 31, 45, 62],
    "effectiveness": "中高 — 但频率偏高（6次/89章），有套路化风险",
    "signature_pattern": "劣势→观察对手→灵光一闪→反转",
    "risk": "⚠️ 已用6次，建议变奏或暂停复用"
  }
]
```

写作时注入："你本小说已验证高效的场景类型：[...] 可复用但注意变奏，标注⚠️的类型建议暂停复用。"

模拟的是真人作者的"场景直觉"——知道武器库里有什么，也知道哪些武器用太多次了。

---

### 维度 4：知识面板的快速编辑能力

**缺口**：双面板设计中，左侧知识面板当前只是展示。但真人作者有时候只想快速改几行字，不想发起一轮完整的讨论。

**方案**：
- 知识面板每个文件以可折叠 Markdown 渲染展示
- 每个文件旁有 `[快速编辑]` 按钮
- 点击后展开一个文本框，直接编辑 Markdown 内容
- 保存时调用 `state_manager.write_knowledge_file()`
- 自动触发一次快照（防误改，但不需要走讨论流程）
- 保存成功后提示"已修改 X.md，下次写作自动生效"

讨论模式适合"想不清楚需要碰撞"的场景，快速编辑适合"我很清楚我要改什么"的场景。

---

### 维度 5：token_tracker 跨小说成本追踪

**缺口**：当前 `token_tracker` 全局累计，开了多本小说后无法区分哪本花了多少钱。

**方案**：扩展现有接口，不改已有签名，只加可选参数：

```python
class Tracker:
    def add_usage(self, agent_name: str, model: str,
                  input_tokens: int, output_tokens: int,
                  cache_hit_tokens: int, is_pro: bool,
                  novel_name: str = None) -> float:
        """novel_name=None → 使用 config.ACTIVE_NOVEL"""

    def get_summary(self, novel_name: str = None) -> dict:
        """
        novel_name=None → 全局汇总
        novel_name="元构魔能" → 单小说汇总
        返回：{total_cost_yuan, call_count, total_cache_hit, total_input}
        """

    def get_all_novels_summary(self) -> dict:
        """返回 {novel_name: summary, ...}，用于前端展示跨小说对比"""
```

内部存储改为 `{novel_name: [records], "__global__": [all_records]}`。

---

### 维度 6：写作微调参数（Writing Knobs）

**缺口**：当前 `user_direction` 是一个自由字符串，真人作者经常需要快速微调"节奏放慢一点""这章多写环境""少写打戏"。LLM 自己从字符串里理解这些意图不稳定。

**方案**：`system_builder` 在 human message 中追加结构化微调块。微调值有两种来源：(a) 用户显式输入中提取 (b) 从最近章节统计自动生成建议：

```markdown
⚙️ 写作微调（以下根据你的输入智能提取，非强制约束）：
   · 节奏：加快 / 保持 / 放慢
   · 对话比例：偏低 / 正常 / 偏高
   · 动作比例：偏低 / 正常 / 偏高（对战/追逐/对抗/行动推进）
   · 环境描写：弱化 / 正常 / 增强（场景氛围、感官细节）
```

四个维度互不重叠，覆盖小说家用直觉微调的主要方向：
- **节奏**：叙事推进速度
- **对话比例**：对话 vs 叙述的比重
- **动作比例**：战斗/追逐/对抗/行动推进场景的占比
- **环境描写**：场景氛围和感官细节的丰富度

用户可以在写作输入框中以自然语言表达（"节奏放慢，多写环境"→ `system_builder` 自动解析为结构化微调），也可以在 UI 中用滑块直接设置。
