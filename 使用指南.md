# NovelNexus V3 使用指南

> 你的专属小说家 — 讨论剧情 · 创作章节 · 持续积累

---

## 一、快速开始

### 1. 配置 API Key

复制环境变量模板并填入你的 DeepSeek API Key：

```bash
cd e:\NovelNexus\NovelNexus_V3
copy .env.example .env
# 编辑 .env，将 DEEPSEEK_API_KEY 设为你的 key
```

### 2. 启动

```powershell
$env:HF_HUB_OFFLINE="1"; $env:TRANSFORMERS_OFFLINE="1"; $env:PYTHONIOENCODING="utf-8"; cd e:\AI_projects\NovelNexus\NovelNexus_V3; & "E:/APP/Anaconda/envs/novelwriter/python.exe" -u web_ui.py
```

> ⚠️ 首次启动会自动下载嵌入模型（约 95MB），建议在终端手动下载确保网络正常：
> ```powershell
> $env:HF_HUB_OFFLINE="1"; $env:TRANSFORMERS_OFFLINE="1"; & "E:/APP/Anaconda/envs/novelwriter/python.exe" -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('BAAI/bge-small-zh-v1.5')"
> ```

浏览器打开 `http://127.0.0.1:7860`。如果端口被占用，系统会自动尝试 7861、7862……直到找到空闲端口。

### 3. 开始创作

页面打开后，左侧知识面板为空文件模板，右侧写作 Tab 如下：

```
第一步：点击「讨论」Tab → 输入你想写的故事方向 → 和 AI 聊设定
第二步：在知识面板确认知识文件已填充后 → 切换到「写作」Tab
第三步：点击「生成」→ AI 写出第一章
第四步：阅读 → 满意点「接受并保存」→ 不满意写原因点「打回重写」
```

---

## 二、界面概览

### 双面板布局

```
┌──────────────────────┬──────────────────────────────┐
│  知识面板（左侧）      │  交互面板（右侧）              │
│                      │                              │
│  📚 小说名、进度      │  ┌────────────────────────┐  │
│  📖 当前章节/卷       │  │  ✍️ 写作 / 💬 讨论      │  │
│  🏠 当前场景          │  │  Tab 切换               │  │
│  📄 世界观.md         │  │                        │  │
│  📄 角色.md           │  │  写作：章节正文 + 附言    │  │
│  📄 大纲.md           │  │  🔍 记忆检索输入框       │  │
│  📄 风格.md           │  │  💬 审查与反馈面板       │  │
│  📄 续写规范.md        │  │  讨论：对话流 + 确认修改  │  │
│                      │  └────────────────────────┘  │
│  ⏪ 回退面板           │  📊 状态栏（进度/费用/记忆段数） │
└──────────────────────┴──────────────────────────────┘
```

### 左侧：知识面板

显示当前小说的全部知识文件。每个文件可折叠展开查看内容。

- **知识文件在 `novels/<小说名>/knowledge/` 下**，是纯 Markdown 文件，可以直接用编辑器修改
- 在 UI 中点「🔄 刷新知识面板」即时生效
- 知识文件的修改**不经过讨论流程**——你改了什么下次写作直接应用

左侧底部的「回退」折叠面板用于撤销章节。

### 右侧：写作 Tab

| 控件 | 作用 |
|------|------|
| 章节正文区域 | 显示生成的章节正文（Markdown 渲染） |
| Agent 附言 / 审查反馈 | 写作附言 + 自动一致性审查结果（检测与历史原文的矛盾） |
| **生成** 按钮 | 调用 AI 写一章 |
| **接受并保存** 按钮 | 确认本章 → 提取摘要 → 更新状态 → 保存到 output/ |
| **撤销** 按钮 | 快速回到上一章（等同于回退到前一个快照） |
| 你的方向 | 可选输入，控制 AI 的方向。留空 = AI 自动续写。**建议只填当前短期规划+临时建议**，永久约束已在续写规范.md 中自动注入 |
| 🔍 记忆检索 | 可选输入，用 `;` 分隔关键词。系统自动在历史章节中搜索相关原文注入写作 prompt |
| 打回重写 | 勾选后写原因，AI 根据原因重新生成当前章 |

### 右侧：讨论 Tab

| 控件 | 作用 |
|------|------|
| 对话流 | 类似 ChatGPT 的多轮对话（Chatbot 组件） |
| 输入框 | 输入你的想法 — "加个设定""改个情节""讨论方向" |
| **讨论** 按钮 | AI 分析影响 → 提供多个方案 → 指出风险 |
| **确认执行修改** 按钮 | AI 的输出中包含可执行方案时，点击即执行 → 修改知识文件 |

---

## 三、写作模式

### 基本流程

```
[用户输入方向（可选）] → [生成] → [阅读正文]
                                     ↓
                        ┌────── 满意 ──────┐
                        ▼                   ▼
                   [接受并保存]         [勾选打回 + 写原因]
                        │                   │
                   ↓                   ↓
              保存到 output/          AI 重写当前章
              下一章 +1
```

### 可选的方向

在"你的方向"输入框中，你可以写任何自然语言指令：

- **留空**：AI 查看最近章节 + 当前状态，自动决定续写内容
- **指定方向**："这章让林野遇到一个突发危机"
- **微调四维**："节奏放慢，多写环境描写"（AI 自动解析为微调参数）
- **节奏快慢、对话多少、动作比例、环境描写**都可以通过一句话来控制
- **短期规划**：从 `我的方向建议.txt` 复制当前短期规划块到方向文本框。**这是最推荐的方式**——短期规划中的"必须发生的剧情"和"⛔ 禁止"清单能精确控制本章走向

> **最佳实践：** 永久约束（世界观基调、战斗描写规范、角色情绪规范等）已全部移入 `续写规范.md`，每次写作自动注入 system prompt。方向文本框不要再重复这些内容，只需要填当前短期规划 + 临时建议即可，让 AI 的注意力集中在当前章节要推进的事情上。

### 打回重写

当你对生成的章节不满意：

1. 勾选「🔄 打回重写」
2. 在「打回原因」框中写清楚哪里不对
3. 点击「生成」

打回原因直接注入到 AI 的 prompt 中，针对性修改。

如果不写原因就点生成，默认提示："请重写，保持衔接但不满意当前版本。"

### Agent 附言与一致性审查

每章写完后，系统会自动执行两项输出：

**1. Agent 附言** — 小说家写完一章后的自我建议：
- **节奏建议**："本章张力偏高，下一章建议安排过渡场景"
- **卷切换信号**：`[卷切换] 第2卷「死线架构师」` — 系统识别后会在接受时自动切换卷
- **矛盾提醒**：`[矛盾提醒] 大纲中第 X 章计划和当前角色状态有冲突，建议讨论`

**2. 一致性审查（第 3 章起自动执行）** — 对比新章节和历史原文检测矛盾：
```
⚠️ 物品矛盾：苏棠的魔能体写成了"手套"，原文是"银色带子"
  ├ 新章写法：她戴上银白色的手套
  ├ 原文写法：一条窄窄的银色带子，绕在手腕上
  └ 💡 建议：改为"她将银色带子绕在手腕上"
```
审查结果同时记录到 `consistency_log.jsonl`，不影响生成流程，仅做提醒。

---

## 四、讨论模式

### 基本流程

```
用户："我觉得应该加一个'元力共鸣'的概念"

AI 分析：
  · 世界观影响 — 是否与现有体系冲突
  · 角色影响 — 推动还是削弱角色弧线
  · 风险提示 — 是否落入俗套

AI 提供 2-3 个方案：
  方案A：情感同步路线，角色关系驱动
  方案B：技术配合路线，智力战斗设计

AI 推荐一个方向，附理由

用户确认或提出修改 → 点「确认执行修改」按钮
  ↓
AI 自动修改知识文件（世界观.md / 角色.md / 大纲.md）
创建讨论前快照（可回退）
记录到 discussion_log.json
  ↓
下次写作时自动应用新设定
```

### 你可以讨论什么

| 类型 | 示例 |
|------|------|
| 添加设定 | "加一个'元力共鸣'的概念" |
| 修改角色 | "我想把苏棠的成长路线改成独立线" |
| 调整节奏 | "最近几章太赶了，我想加入日常过渡" |
| 讨论伏笔 | "第 20 章埋的伏笔我想在第 50 章回收" |
| 改变方向 | "我觉得林野不应该这么强，让他在第 3 章输一次" |
| 任意对话 | "我问你个问题..." |

### 讨论模式输出的结构

```
AI 回复（自然语言分析）
  → 可选包含 modification_plan JSON 结构：
    { "analyses": [...], "options": [...], "recommendation": "..." }
  → 点击「确认执行修改」后执行第一个方案
  → 执行前会在聊天中显示修改预览
    「📋 即将执行修改（方案A）：
      · 世界观.md：新增'元力共鸣'...
      · 角色.md：周瀚新增与林野的潜在共鸣搭档关系...」
```

---

## 五、多小说管理

### 切换小说

左侧顶部的下拉菜单列出所有已有小说。切换即生效：

```
选择小说 → 自动清空当前未接受的章节 → 加载该小说的知识文件 + 状态
```

### 创建新小说

左侧「✨ 新小说」折叠面板：

| 字段 | 说明 |
|------|------|
| 小说名 | 必填，作为 novels/ 下的目录名 |
| 核心创意 | 你想写什么样的故事？一大段话也可以 |
| 主角 | 主角名字 + 一句话人设 |

点击「创建」后自动：

1. 创建 `novels/小说名/knowledge/` 目录及模板文件（世界观.md、角色.md、大纲.md、风格.md、续写规范.md）
2. 初始化 state.json
3. 切换到该新小说

然后进入「讨论」Tab，和 AI 聊设定，填充知识文件。

### novels/ 目录结构

```
novels/
├── 元构魔能/
│   ├── knowledge/
│   │   ├── 世界观.md              # 世界规则、力量体系
│   │   ├── 角色.md                # 角色设定
│   │   ├── 大纲.md                # 卷级弧线 + 里程碑
│   │   ├── 风格.md                # 写作风格规范
│   │   ├── 续写规范.md            # 续写硬约束
│   │   ├── state.json             # 动态状态（进度/位置/伏笔/悬念）
│   │   ├── summaries.json         # 章节摘要链
│   │   ├── discussion_log.json    # 讨论记录
│   │   ├── style_progression.json # 风格演化记录
│   │   └── snapshots/             # 快照 + 事件时间线
│   └── output/                    # 生成的章节 markdown
└── 另一本小说/
    └── ...
```

---

## 六、回退

### 回退操作

左侧底部「⏪ 回退」折叠面板：

1. **快照章节**下拉框：列出所有已保存的快照
2. **撤销上一章**按钮：快速回到前一个快照
3. **回退到选定章节**按钮：回退到指定章节
4. **同时撤销中间讨论**复选框：勾选后，自动恢复被回退范围内的讨论前知识文件

### 回退包含什么

| 回退类型 | 效果 |
|---------|------|
| 仅回退章节 | 删除被回退的 output 章节文件 + 快照。保留讨论记录的设定修改 |
| 同时撤销讨论 | 上述 + 从 `disc_XXX_pre.json` 恢复知识文件到讨论前的版本 |

### 时间线机制

所有操作（写章节、讨论修改、回退）自动记录到 `events.jsonl`。回退时扫描此文件，展示影响范围。

---

## 七、写作微调四维

### 四维说明

| 维度 | 三档值 | 影响 |
|------|--------|------|
| 节奏 | 加快 / 保持 / 放慢 | 叙事推进速度 |
| 对话比例 | 偏低 / 正常 / 偏高 | 对话 vs 叙述的比重 |
| 动作比例 | 偏低 / 正常 / 偏高 | 战斗/追逐/对抗/行动推进场景 |
| 环境描写 | 弱化 / 正常 / 增强 | 场景氛围和感官细节 |

### 使用方式

在写作 Tab 的「你的方向」输入框中直接说：

```
"节奏放慢，多写环境"
"这章少写对话，加快节奏"
"动作比例偏高，环境描写弱化"
```

AI 自动从自然语言中解析出微调参数，在 human message 中生成结构化微调块：

```
⚙️ 写作微调（非强制约束）：
   · 节奏：放慢
   · 对话比例：偏低
   · 动作比例：偏高
   · 环境描写：增强
```

---

## 八、向量记忆与检索

向量记忆贯穿**写作前、写作后、讨论时、保存时**四个阶段。下面把每一步谁做什么梳理清楚。

### 流程总览

```
启动时 NovelMemory.rebuild_index()
  → 遍历 output/ 第001章~第NNN章.md
  → 切段落（每段 ≥50 字，截断 800 字）
  → 用 sentence-transformers 生成向量（BAAI/bge-small-zh-v1.5）
  → 存到 ChromaDB（EphemeralClient，内存模式）
  → 完成索引，Web UI 启动
```

之后每次写作，经过四个阶段：

```
 写作前                             写作后                             保存时
 ┌────────────────┐               ┌────────────────┐               ┌──────────────┐
 │① 自动检索       │  → Novelist   │② 一致性审查     │  → UI展示     │③ 向量索引     │
 │   generate()    │  (pro) 写正文 │   check_       │     正文      │   accept()    │
 │   129-144行     │               │   consistency() │     冲突提示   │   471-476行    │
 │                 │               │   270-291行     │     标题修正   │               │
 │④ 讨论回忆       │  ← 也可手动   │③ 标题审查       │               │               │
 │   discuss()     │  🔍 记忆检索  │   _check_      │               │               │
 │   546-557行     │  覆盖自动检索  │   title()       │               │               │
 └────────────────┘  145-155行    │   120-171行     │               │               │
                                  └────────────────┘               └──────────────┘
```

---

### 阶段一：写作前——自动检索（谁做什么）

**谁执行：** `novelist.py` → `generate_chapter()` 第129-144行

**执行时机：** 在组装 human prompt 之前，调用 LLM 之前

**步骤：**

```
① 从 state["characters"] 读取所有角色名（如林野、苏棠、方林、沈鸣...）
   最多取前 10 个角色

② 对每个角色名构造查询字符串：
   "关于{林野}的描写、外貌、行为、对话、动作"

③ 如果 state["current_scene"] 不为空，追加一条场景查询

④ 取前 6 条查询，每条搜索 ChromaDB 取 top-3（最多 18 条片段）

⑤ 去重（同一章节同一段落不重复），按章节号排序

⑥ 最多保留 VECTOR_MEMORY_SEARCH_K × 3 条（默认 15 条）

⑦ 注入 `build_writer_human()` 的 prompt 中，格式：
   【检索到的相关原文片段】
     [第14章] 沈鸣双手插在口袋里，站在围栏外面...
     [第14章] "你看到的是什么？"沈鸣问...
```

**映射：** `system_builder.py` 第162-173行 → `build_writer_human()` 把 `recall_snippets` 渲染为原文片段块

**效果：** 写第15章前，AI 已经看到「第14章沈鸣和林野对话过」，不会再让林野"不认识沈鸣"

---

### 阶段一补充：手动检索覆盖

**谁执行：** `generate_chapter()` 第145-155行

```
if 用户在 🔍 输入框填了关键词:
   用用户的关键词搜索 ➔ 完全替换自动检索的结果
if 没填:
   保持自动检索的结果不变
```

**规则：** 手动检索的结果数 = `VECTOR_MEMORY_SEARCH_K × 2`（默认10条），自动检索 = `× 3`（默认15条）

---

### 阶段二：写作后——一致性审查（谁做什么）

**谁执行：** `consistency_checker.py` → `check_consistency()`

**谁调用：** `novelist.py` → `generate_chapter()` 第264-285行

**执行时机：** Novelist(pro) 写完正文之后，返回 UI 之前

**触发条件：** `VECTOR_MEMORY_ENABLED=true` + `chapter_num > 2` + 正文 > 200 字

**步骤：**

```
① 从正文中提取关键词：
   · 角色名（从 state.json + 角色.md 动态读取）
   · 物品名（正则提取「的XX」结构）

② 用每个关键词去 ChromaDB 搜索，每条取 top-3

③ 取前 5 条关键词（最多 15 条原文片段）

④ 构造审查 prompt，发给 flash 模型（Extractor 同款）：
   【审查系统指令】
   对比新正文 vs 原文片段，找事实性矛盾
   【原文片段】
   [第14章] 沈鸣双手插在口袋里...
   【新正文开头 2000 字】
   ...

⑤ flash 模型输出 JSON：
   {"has_conflict": true/false, "conflicts": [...], "summary": "..."}

⑥ 如果有矛盾，格式化为多行警告文本

⑦ 记录到 novels/<小说名>/knowledge/consistency_log.jsonl
```

**不做什么：** 不打断生成流程，不做自动修正，仅做提醒

**技术细节：** 使用 flash 模型（temperature=0.3，response_format=json_object），审查消耗约 2000-4000 input tokens

---

### 阶段三：写作后——标题审查（谁做什么）

**谁执行：** `novelist.py` → `_check_title()` 第124-171行

**谁调用：** `generate_chapter()` 第288-292行

**执行时机：** 一致性审查之后，返回 UI 之前

**触发条件：** `chapter_num > 1` + 正文 > 200 字

**步骤：**

```
① 从正文中提取标题（正则匹配 `# 第XX章 XXX`）

② 取正文前 3000 字作为判断依据

③ 发给 flash 模型：
   【标题审查指令】
   标题：失控的T2
   正文开头：...
   判断标题是否匹配正文 → JSON输出

④ flash 输出：
   {"match": false, "suggested_title": "短剑初试"}

⑤ 如果判定不匹配 + 有建议标题 + 长度 ≤20 字：
   用正则替换标题
   返回 {"original_title": "失控的T2", "new_title": "短剑初试", "rewritten": true}

⑥ 如果判定匹配：不做任何修改
```

**渲染：** 在 Web UI 的「审查与反馈」面板显示 `✏️ 标题已修正：「失控的T2」→「短剑初试」`

---

### 阶段四：保存时——向量索引（谁做什么）

**谁执行：** `novelist.py` → `accept_chapter()` 第471-476行

**谁调用：** 用户在 Web UI 点击「接受并保存」

**执行时机：** 正文保存到 output/ + state.json + summaries + 快照之后

**步骤：**

```
① 把正文按 \n\n 切段落
② 过滤：小于 50 字的段落跳过
③ 截断：每段最多 800 字
④ 生成段落 ID：ch015_p0003（第15章第3段）
⑤ 去重：已有 ID 跳过（相同段落不会重复索引）
⑥ 批量存入 ChromaDB：
   collection.add(
     ids=[...],
     documents=[段落文本],
     metadatas=[{"chapter": 15, "paragraph_index": 3}]
   )
⑦ 下次写作时自动检索就能搜到本章内容
```

**不做什么：** 不覆盖已索引的段落（增量追加）

---

### 阶段五：讨论时——自动回忆（谁做什么）

**谁执行：** `novelist.py` → `discuss()` 第546-557行

**执行时机：** 用户在讨论 Tab 发送消息时

**触发条件：** 用户消息中包含触发词：
```
样子 | 什么颜色 | 长什么样 | 之前 | 第几章 |
回忆 | 描写 | 外貌 | 装备 | 武器 | 细节 | 特征 | 什么样 | 还记得
```

**步骤：**

```
① 检查用户输入是否含触发词
② 如果含触发词：
   memory.search(user_message, k=10)  → 用用户整句话搜索
③ 注入 build_discussion_prompt()：
   【检索到的相关原文片段】
     [第8章] 苏棠的魔能体是一条窄窄的银色带子，绕在手腕上...
```

---

### 一句话总结

| 阶段 | 谁 | 做什么 | 耗时 | 影响 |
|------|---|--------|------|------|
| **写作前** | `generate_chapter()` | 自动用角色名搜向量库 → 注prompt | ~200ms | AI 写之前就记得 |
| **写作后** | `check_consistency()` | flash 对比新正文 vs 历史原文 | ~500ms | 检出矛盾，提醒你 |
| **写作后** | `_check_title()` | flash 判断标题是否匹配正文 | ~300ms | 不匹配则自动改写 |
| **保存时** | `accept_chapter()` | 新章节切段 → 存 ChromaDB | ~100ms | 下次能搜到 |
| **讨论时** | `discuss()` | 检测触发词 → 搜向量库 → 注prompt | ~200ms | 讨论时也能回忆 |

---

## 九、知识文件编写指南

### 世界观.md

建议包含：力量体系、世界规则、历史背景、地理设定。

```
# 世界观

## 力量体系
魔力源自...分为...

## 世界规则
1. 规则一
2. 规则二

## 历史背景
...

## 主要势力
- 势力A：...
- 势力B：...
```

### 角色.md

每个角色一个段落，包含身份、性格、语言习惯。

```
# 角色

## 林野
- 身份：...
- 性格：...
- 语言习惯：短句，爱反问

## 苏棠
- 身份：...
- ...
```

### 大纲.md

只给卷级弧线和里程碑，不绑章节号。

```
# 大纲

## 第一卷：卷名
- **卷级弧线**：主角从 X 到 Y 的核心变化
- **情感基调**：压抑→觉醒→首胜

### 必须里程碑（不绑定章节号）
1. 主角发现自己的特殊能力
2. 第一次实战
3. 遇到第一个重要盟友
4. 卷末：意识到更大的世界

## 第二卷：...
```

### 风格.md

写作风格偏好。如果没有特别指定，系统会持续推进并在每卷结束后自动从实际产出中蒸馏风格特征。

```
# 风格

## 句式偏好
- 短句为主，偶尔长句制造节奏变化
- 对话简短有力，少用语气词

## 感官描写
- 优先触觉和温度
- ...
```

---

## 十、配置说明

主要配置通过 `.env` 文件完成。

### 三接口独立配置

| 接口 | 用途 | 默认模型 | 建议 |
|------|------|---------|------|
| **novelist** | 写作 + 讨论 | `deepseek-v4-pro` | 创作质量优先，不改 |
| **extractor** | 章节提取摘要 | `deepseek-v4-flash` | 便宜模型即可 |
| **compressor** | 卷级压缩/日志压缩 | `deepseek-v4-flash` | 同上 |

每个接口支持以下参数（均可在 `.env` 中覆盖）：

| 参数 | 取值范围 | 说明 |
|------|---------|------|
| `MODEL_TEMPERATURE` | 0 ~ 2 | 采样温度 |
| `MODEL_MAX_TOKENS` | 1 ~ 模型上限 | 最大输出 token |
| `MODEL_TOP_P` | 0 ~ 1 | 核采样 |
| `MODEL_FREQUENCY_PENALTY` | -2 ~ 2 | 重复惩罚 |
| `MODEL_PRESENCE_PENALTY` | -2 ~ 2 | 话题新鲜度 |
| `MODEL_ENABLE_THINKING` | true/false | 思考模式 |
| `MODEL_REASONING_EFFORT` | low/medium/high | 思考深度 |

### 写作参数

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `CHAPTER_WORD_COUNT_MIN` | 2200 | 每章最低字数 |
| `CHAPTER_WORD_COUNT_MAX` | 2800 | 每章最高字数 |
| `CONTEXT_SOFT_LIMIT_TOKENS` | 30000 | 超过此值触发二次压缩 |
| `RECENT_CHAPTERS_COUNT` | 2 | 最近全文章节数 |

---

## 十一、成本估算

按 DeepSeek 官方定价（¥/M tokens）：

| 场景 | Token 量 | 成本 |
|------|---------|------|
| 写一章（缓存命中） | ~15,000 system + 8,000 human + 3,000 output | **~¥0.029** |
| 写一章（缓存未命中） | 同上 | ~¥0.039 |
| 提取摘要 | ~6,000 input + 500 output | ~¥0.014 |
| 讨论一次 | ~10,000 input + 1,000 output | ~¥0.023 |

实际每章成本平均约 **¥0.03-0.04**。缓存命中越多越接近 ¥0.03。

每 50 章约 **¥1.5-2.0**。

---

## 十二、注意事项

### 写前必读

1. **第一次使用时，先在讨论 Tab 和 AI 聊设定**。知识文件填充越完整，小说家的写作质量越高
2. **大纲只给粗略路线**——卷级弧线 + 不绑章节号的里程碑。不要写"第 47 章：林野遇到 X"这种精度，会变成枷锁
3. **开局时先手动写世界观.md 和角色.md**——AI 在这些内容上的第一次生成质量直接受输入质量影响

### 写作中

4. **Agent 附言的矛盾提醒值得注意**——小说家有时会主动发现大纲和当前状态的矛盾
5. **一致性审查仅做提醒，不影响生成流程**——检测到矛盾不会打断写作，修改与否由你决定
6. **打回原因越具体效果越好**——"对话生硬"不如"苏棠不会这么直接，她习惯拐弯抹角"
7. **经常刷新知识面板**——每次讨论修改后，刷新确认修改已生效

### 进阶

8. **快照自动生成本身不是消耗** —— `accept_chapter` 时自动创建，`ch_001.json` 快照约 10KB
9. **可以手动编辑任意知识文件**——用编辑器修改 `novels/<小说名>/knowledge/` 下的 md 文件即可，不需要经过 UI
10. **Pricing 字典中的价格需要根据官网实时更新**——DeepSeek 定价可能调整，定期查看 [DeepSeek API Pricing](https://api-docs.deepseek.com/zh-cn/quick_start/pricing)

### 向量记忆

11. **首次启动会索引所有历史章节**——26 章约 10 秒完成，之后增量索引新章节
12. **写作前自动检索已覆盖常见遗忘场景**——系统自动根据 state 角色名搜索向量库注入 prompt，不需要手动干预
13. **「🔍 记忆检索」手动输入覆盖自动检索**——输入后自动检索被忽略，完全按你的关键词搜索
14. **一致性审查仅限第 3 章及以后**——前 2 章没有足够的历史原文做对比
15. **标题自动审查修正**——如果标题和内容不匹配，flash 模型自动重写，在反馈面板显示修正记录

### 常见操作细节

14. **讨论 Tab 和写作 Tab 的上下文是隔离的** —— 写作时的小说家只能看到知识文件，看不到讨论对话。讨论中达成的任何共识（选了哪个方案、确认了哪种写法、修正了什么理解）必须写入知识文件，否则写作时小说家不知道。文件选择规则：选择了开篇方案/章节写法/表达修正 → 写入续写规范.md；修改了世界观规则/新增设定 → 写入世界观.md；调整了角色性格/背景 → 写入角色.md；改了故事走向/里程碑 → 写入大纲.md
15. **打回重写不是单独按钮** —— 勾选「🔄 打回重写」→ 写下原因 → 点「🚀 生成」。同一个按钮，勾了就是打回，不勾就是续写下一章
16. **接受的章节保存到四处**：output/ 正文、state.json（下一章 +1 + 角色位置更新）、summaries.json（摘要链）、snapshots/（快照）——同时落地
17. **回退时角色问题**：仅回退章节 → state.json 里的角色位置回到旧版本，但角色.md 不动（新角色设定仍在）；勾选同时撤销讨论 → 角色.md 也恢复到讨论前
18. **角色有两个来源**：角色.md 是"这个人是谁"（静态设定），state.json 的 characters 是"这个人现在在哪在干嘛"（动态坐标，每章提取器自动更新）。两者同时注入 system prompt，小说家既有深度理解又有实时坐标
19. **「你的方向」在注意力最高区** —— 写入 human message 末尾，LLM 一定会看到它；留空 AI 自动续写，写了就按方向走
20. **Agent 附言里同时出现的卷切换和矛盾提醒** —— 系统会自动识别 `[卷切换]` 和 `[矛盾提醒]`，接受章节时自动处理，不需要你手动操作
21. **等待生成时不要重复点击按钮** —— Gradio 不支持同时发起多次调用，如果卡住了先等 30 秒再刷新

### 局限

22. **第一章没有上章可衔接**——如果第一次使用时知识文件为空，生成质量会显著下降。建议先填至少世界观和角色
23. **连续打回多次时注意**——每次打回都消耗 token（重新生成），且质量不一定单调递增。如果连续 3 次不满意，建议停下来用讨论模式聊清楚再写
24. **极端长篇小说（500+ 章）首次遇到卷切换时**——卷级压缩函数当前为存根，卷历史记录仍可正常工作
