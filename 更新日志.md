# 2026.5.11

***

## V3 → V4 重构调研报告 & 改动计划

### 一、问题诊断

#### 1.1 核心痛点

通过分析 V3 在「元构魔能」项目（26章已完成）上的实际表现，发现根本问题：

**V3 的记忆是"摘要式"而非"回查式"。**

当写到第27章时，AI 看到的上下文是：

- `summaries.json` 的摘要（150字/章 → 细节大量丢失）
- `state.json` 的角色状态（只有位置/状态，无外貌/物品细节）
- 最近2章原文（其余章节原文不加载）

**导致的典型丢失：**

- 苏棠的魔能体是"银色带子绕手腕"，summaries 只记了"T1魔能体"
- 角色是"一年级"，summaries 只记了"院级荣誉生"
- 食堂的"老槐树"、"青藤廊道"等场景细节无法被后续章节引用

#### 1.2 现有 Extractor 机制的局限性

V3 的 Extractor（基于 `deepseek-v4-flash`）是一个"情节分析器"，不是"细节记录器"：

```
EXTRACTOR 关注的点：
✅ 情节（发生了什么）
✅ 角色位置（谁在哪）
✅ 悬念（钩子）
✅ 伏笔（铺垫/回收）
✅ 新结构/新角色

EXTRACTOR 忽略的点：
❌ 外貌细节（如银色带子）
❌ 环境描写（如老槐树的树荫）
❌ 物品特征（如旧手套上的铜绿和裂缝）
❌ 年级/身份等元数据
❌ 可呼应的意象/母题
```

**核心洞察：靠不断增加 Extractor 的 JSON 字段来覆盖所有可能的细节类型，是一条走不到头的路。** 小说细节的类型是开放式的——今天是"银色带子"，明天可能是"食堂阿姨的围裙颜色"、"走廊上第三块翘起的石板"。你不可能枚举所有"可能需要记住的细节"。

***

### 二、调研结论

#### 2.1 技术路线对比

调研了 GitHub 上 10+ 个开源 AI 小说项目、1 篇学术论文（北京交通大学）、多个技术博客：

| 项目                             | Stars | 记忆方案                            | 与V3对比        |
| ------------------------------ | ----- | ------------------------------- | ------------ |
| **ReNovel-AI**                 | \~100 | ChromaDB向量库 + SQLite元数据         | V3缺向量检索层     |
| **AI-Novel-Writing-Assistant** | 1.1k  | RAG + Agent + 世界设定引擎            | V3缺RAG       |
| **NovelForge**                 | 717   | JSON Schema结构化生成 + 卡片式编辑        | V3缺结构化约束     |
| **AI\_NovelGenerator**         | \~200 | **三级分层记忆**：长期库/中期库/短期库          | V3只有一级       |
| **AI Novelist RAG**            | \~0   | FAISS + BERT摘要                  | V3用LLM做摘要类似  |
| **51mazi（我要码字）**               | 269   | Electron桌面端 + 人物档案 + 关系图谱 + 时间线 | V3缺可视化图谱     |
| **oh-story-claudecode**        | 743   | Claude Code Skills + 文件系统       | 不同路线（skill包） |
| **学术论文(2025)**                 | -     | **知识图谱** + 长短期记忆 + 文学理论驱动       | V3缺图谱结构      |

#### 2.2 人类作家 vs AI 记忆机制分析

人类作家写到后面需要前面细节时的实际行为：

| 行为      | 人类做法                | V3模拟        |
| ------- | ------------------- | ----------- |
| 按关键词翻查  | "在文档里搜'苏棠'" → 找到第8章 | ❌ 无全文检索     |
| 按场景回溯   | "翻目录/大纲 → 快速浏览第9章"  | ⚠️ 只有摘要，无原文 |
| 按意象联想   | "银色带子出现几次了？可以再呼应"   | ❌ 无意象追踪     |
| 查角色设定笔记 | "看看角色.md里苏棠的描述"     | ✅ 知识文件可查    |

**核心发现：人类作家不是"记住所有细节"，而是"知道如何快速找到需要的细节"。**

V3 模拟人类记忆的评分：**5/10**。缺失的不是"记忆力"，而是"回查原文的能力"。

#### 2.3 核心结论

**结论一：纯 RAG 不是银弹，但"Layer 2 按需检索"是不可或缺的能力。**

- 纯 RAG（全程向量检索）的缺陷：语义相似 ≠ 叙事关联；不知道什么是"重要的"；丢失了结构化组织
- 但"需要时回到原文查"的能力是必须的——这模拟了人类作家"翻前文章节"的行为

**结论二：V3 的"结构化知识层"（summaries + state + 角色.md）是正确的 Layer 1，不需要推倒。**

- 所有成功的项目都保留了这一层
- 它提供了"什么是重要的"这个叙事视角，这是纯 RAG 做不到的

**结论三：短期可行的改进路径是三层记忆架构，而非无限增加字段。**

```
Layer 1: 核心知识（V3已有，小修）   → 世界观/角色/大纲/state/summaries
                                    → 补充 physical_details / grade_info 等字段
Layer 2: 按需检索（V3缺失，关键）   → ChromaDB 向量全文索引
                                    → 写作/讨论时随时回到原文查找
Layer 3: 叙事图谱（远期，学术方向） → 知识图谱追踪意象/关系/伏笔
                                    → 自动发现跨章节呼应机会
```

**结论四：Layer 2 是改动最小、收益最高的改进，V4应以此为核心。**

- ChromaDB 是本地运行的开源库，零额外 API 成本
- 不需要改变 V3 现有的 prompt 构建流程
- 只需在写作/讨论时增加一个"检索 → 注入"步骤
- 与 DeepSeek 前缀缓存兼容（检索结果放在 prompt 末尾，不破坏前面的稳定内容）

***

### 三、详细改动计划

### Phase 1: Layer 2 —— 按需全文检索（核心改动）

#### 3.1 新增文件：`novel_memory.py`

```
NovelNexus_V3/
├── novel_memory.py              # 新增：向量全文检索模块
```

**功能清单：**

| 方法                                 | 说明                                  |
| ---------------------------------- | ----------------------------------- |
| `index_chapter(chapter_num, text)` | 在 `accept_chapter` 时自动调用，将新章节分段索引入库 |
| `search(query, k=5)`               | 语义检索相关段落，返回原文片段 + 所在章节              |
| `search_keyword(keyword)`          | 精确关键词搜索（ChromaDB `$contains` 过滤）    |
| `recall_for_character(name)`       | 检索某角色在各章节的出场片段                      |
| `recall_for_item(name)`            | 检索某物品/意象在各章节的出现                     |
| `rebuild_index()`                  | 从所有已有章节重建索引（启动时或首次使用时调用）            |
| `get_stats()`                      | 返回索引规模统计（分块数、覆盖章节数）                 |

**存储位置：** `novels/{小说名}/memory_db/`（ChromaDB 持久化目录）

**分块策略：**

- 按段落分块（`\n\n` 分隔）
- 过滤长度 < 50 字的段落（避免单行对话碎片化）
- 每块携带 metadata：`chapter`（章节号）、`paragraph_index`（段落序号）

#### 3.2 修改文件：`requirements.txt`

添加依赖：

```
chromadb>=0.5.0
sentence-transformers>=2.2.0   # 本地 embedding 模型，无需 API 调用
```

**embedding 选型：**

- 默认使用 `all-MiniLM-L6-v2`（384维，80MB，本地运行，零API成本）
- 中文场景可选 `BAAI/bge-small-zh-v1.5`（512维，中文优化）
- 通过 .env 可切换 `VECTOR_MEMORY_MODEL` 切换模型

#### 3.3 修改文件：`config.py`

新增配置项：

```python
# ── Vector Memory (V4新增) ──
VECTOR_MEMORY_ENABLED = os.getenv("VECTOR_MEMORY_ENABLED", "true").lower() in ("true", "1", "yes")
VECTOR_MEMORY_MODEL = os.getenv("VECTOR_MEMORY_MODEL", "all-MiniLM-L6-v2")
VECTOR_MEMORY_SEARCH_K = int(os.getenv("VECTOR_MEMORY_SEARCH_K", "5"))
```

#### 3.4 修改文件：`novelist.py`

**改动点 A：`accept_chapter()`** **末尾添加索引步骤**

```python
def accept_chapter(...):
    # ... 现有提取逻辑（不变） ...
    
    # 新增：索引新章节到向量数据库
    if config.VECTOR_MEMORY_ENABLED:
        from novel_memory import NovelMemory
        memory = NovelMemory(config.ACTIVE_NOVEL)
        memory.index_chapter(chapter_num, chapter_text)
```

**改动点 B：`generate_chapter()`** **支持可选的记忆检索**

```python
def generate_chapter(user_direction="", recall_queries=None, ...):
    # ... 现有构建逻辑 ...
    
    # 新增：如果提供了检索查询，执行向量检索并注入 prompt
    if recall_queries and config.VECTOR_MEMORY_ENABLED:
        from novel_memory import NovelMemory
        memory = NovelMemory(config.ACTIVE_NOVEL)
        recall_snippets = []
        for query in recall_queries:
            results = memory.search(query, k=config.VECTOR_MEMORY_SEARCH_K)
            for r in results:
                recall_snippets.append(
                    f"[第{r['chapter']}章] {r['text'][:300]}"
                )
        if recall_snippets:
            human += "\n\n【记忆检索 — 相关原文片段】\n" + "\n---\n".join(recall_snippets)
    
    # ... 继续现有调用逻辑 ...
```

**改动点 C：`discuss()`** **中支持记忆检索**

讨论模式下，当用户询问细节问题时，自动触发向量检索：

```python
def discuss(user_input, ...):
    # 自动检测是否包含查询意图关键词
    recall_triggers = ["样子", "什么颜色", "长什么样", "之前", "第几章", "回忆"]
    should_recall = any(t in user_input for t in recall_triggers)
    
    if should_recall and config.VECTOR_MEMORY_ENABLED:
        from novel_memory import NovelMemory
        memory = NovelMemory(config.ACTIVE_NOVEL)
        snippets = memory.search(user_input, k=5)
        # 注入到讨论 prompt 中
```

#### 3.5 修改文件：`web_ui.py`

- 在讨论模式界面添加 `recall` 命令按钮/输入框
- 在写作模式添加"检索指定细节"输入框（可选）
- 首次加载时自动执行 `rebuild_index()`（如果索引为空且已有章节文件）

#### 3.6 修改文件：`system_builder.py`

- `build_writer_human()` 支持可选的 `recall_snippets` 参数
- `build_discussion_prompt()` 在讨论 prompt 中注入检索到的原文片段

***

### Phase 2: Layer 1 加强 —— Extractor 字段补丁（辅助改动）

虽不应无限增加字段，但少量关键字段补充可提升摘要质量：

#### 2.1 修改 `EXTRACTOR_SYSTEM` in `system_builder.py`

在 JSON 结构中新增两个字段：

```json
{
  "physical_details": [
    {
      "character": "角色名",
      "item": "物品/装备名",
      "appearance": "外貌/外观描述",
      "chapter": 当前章节号
    }
  ],
  "grade_info": {
    "characters": ["角色名(年级)", ...]
  }
}
```

**储存位置：** 新增 `physical_details.json`（`novels/{小说名}/knowledge/physical_details.json`），独立于 summaries.json，专门存储外貌和物品细节。

#### 2.2 修改 `state_manager.py`

新增函数：

- `load_physical_details()` 读取细节文件
- `save_physical_details()` 写入细节文件
- `get_physical_details_for_character(name)` 按角色查询

***

### Phase 3: Layer 3 展望 —— 叙事知识图谱（远期）

研究论文（Shi et al., 2025）证明，知识图谱能显著提升长故事的主题一致性。

**远期目标：**

```
节点类型:
  Character → 苏棠、林野、周瀚...
  Item      → 银色带子(T1)、旧手套(T3)、短距连接线...
  Location  → 第七训练场、食堂、青藤廊道、后山墙外...
  Event     → 苏棠挑战林野(Ch.8-9)、蚀元兽遭遇(Ch.12)...
  Concept   → 结构效率 > 数量、共享坐标点...

关系类型:
  owns      → 苏棠 -[拥有]-> 银色带子
  appears   → 银色带子 -[出现于]-> Ch.8 / Ch.10 / Ch.24
  challenges→ 苏棠 -[挑战]-> 林野
  befriends → 苏棠 -[成为伙伴]-> 林野 (Ch.10后)
  located   → 对决 -[发生于]-> 第七训练场
```

**实现方式（可选）：**

- 轻量方案：`networkx` 图结构 + JSON 序列化
- 生产方案：Neo4j / ArangoDB
- 最简方案：扩展 `physical_details.json` 为关系表格式

***

## 四、参考项目与文献

### 4.1 GitHub 开源项目

| 项目                             | 链接                                                                                         | 亮点                                                          |
| ------------------------------ | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------- |
| **AI-Novel-Writing-Assistant** | <https://github.com/ExplosiveCoderflome/AI-Novel-Writing-Assistant>                        | 1.1k Stars，TypeScript+Python，Agent+RAG+世界观引擎，支持分卷规划与异步向量检索  |
| **NovelForge**                 | <https://github.com/RhythmicWave/NovelForge>                                               | 717 Stars，Python，基于 JSON Schema 的结构化生成 + 卡片式创作              |
| **oh-story-claudecode**        | <https://github.com/worldwonderer/oh-story-claudecode>                                     | 743 Stars，Claude Code Skills 包，覆盖扫榜/拆文/写作/去AI味全流程           |
| **ReNovel-AI**                 | <https://github.com/BiranSama/ReNovel-AI>                                                  | ∼100 Stars，ChromaDB向量库 + 三模态AI(Writers/Reviewer/Chat)，段落级精修 |
| **51mazi（我要码字）**               | <https://github.com/xiaoshengxianjun/51mazi>                                               | 269 Stars，Electron桌面端，人物档案+关系图谱+时间线+AI写作                    |
| **AI\_NovelGenerator**         | <https://github.com/Novel-AI/AI_NovelGenerator>                                            | ∼200 Stars，三级分层记忆体系（长期/中期/短期库），支持百万字长篇强一致性                  |
| **AI Novelist RAG**            | <https://github.com/nanfang-wuyu/AI-Novelist-RAG---Long-Form-Story-Generation-with-Memory> | FAISS+BERT摘要 + LangChain + Gradio，学术向实践                     |

### 4.2 学术论文

| 论文                                                                | 来源                                                | 核心贡献                                                                                                 |
| ----------------------------------------------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| **Long Story Generation via Knowledge Graph and Literary Theory** | Shi, Huang, Feng (2025), 北京交通大学, arXiv:2508.03137 | 提出知识图谱 + 长短期记忆 + 文学理论驱动的多 Agent 故事生成框架。引入"theme obstacle"概念，通过知识图谱节点引入和相似度计算来增强情节吸引力，有效解决主题漂移和情节单调问题 |

### 4.3 技术博客

| 文章                                                     | 链接                                                                | 要点                                                                                                    |
| ------------------------------------------------------ | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Best AI for Writing a Novel with Character Consistency | <https://blog.bookautoai.com/best-ai-for-writing-novel/>          | 提出"place to store facts + way to recall facts + feedback loop to correct drift"三要素；微摘要、场景标签、权威更新的实践建议 |
| AI\_NovelGenerator全指南                                  | <https://blog.csdn.net/zsh_1314520/article/details/160830617>     | 详细介绍三级分层记忆（长期库/中期库/短期库）的实现原理                                                                          |
| RAG完整技术方案                                              | <https://blog.csdn.net/weixin_46041161/article/details/151873601> | ChromaDB + LangChain RAG实践，文本分块策略（500-1500 tokens，10-20%重叠）                                           |

***

### 五、实施顺序建议

```
Phase 1 (V4.0): Layer 2 —— novel_memory.py + ChromaDB
    ├── 3.1 新增 novel_memory.py
    ├── 3.2 修改 requirements.txt
    ├── 3.3 修改 config.py
    ├── 3.4 修改 novelist.py (index + search + discuss)
    ├── 3.5 修改 web_ui.py (添加 recall UI)
    └── 3.6 修改 system_builder.py

Phase 2 (V4.1): Layer 1 小修 —— physical_details + grade_info
    ├── 2.1 修改 EXTRACTOR_SYSTEM
    └── 2.2 修改 state_manager.py

Phase 3 (V5.0): Layer 3 叙事图谱（待评估）
    └── 知识图谱（networkx / Neo4j）
```

<br />

# 2026.5.16

## 核心改动

### 角色装备结构化
- extractor 输出 `equipment_updates` 字段，解析每章角色装备信息
- state.json `characters[name].equipment` 持久化
- prompt 中角色位置行显示装备信息（`[glove=T3 魔能体=T1]`）

### 伏笔结案增强
- 关键词交集匹配 + item name 子串回退双通道
- 解决"关键词不一致导致伏笔无法自动结案"问题

### 回忆规划层
- 新增 `_plan_and_recall()`：写前用 flash 模型规划写作方向和检索需求
- prompt 新增【写作计划】【确认的事实】区块
- 规划失败自动回退旧式散装 auto queries

### 精确确认（D方案）
- 新增 `_confirm_fact()` 三阶段下探：向量语义确认 → 摘要关键词 → 原文逐行搜索
- 新增 `CONFIRMATION_PROMPT`，LLM 二次判断检索结果是否真正回答问题

### 向量记忆时序加权（B方案）
- `search()` 返回前对 distance 做 `*(1.0 - boost * chapter/max_chapter)` 加权
- 最新章节语义相似的段落优先召回

### 官方 tokenizer 集成
- 新建 `token_counter.py`，使用 DeepSeek 官方 tokenizer
- `estimate_tokens` 改为真实 token 计数（回退 `len*1.5`）

### Cache hit 追踪修复
- `_extract_usage` 合并 `usage_metadata` 和 `response_metadata.token_usage` 两个来源
- 确保 DeepSeek 的 `prompt_cache_hit_tokens` 不被遗漏

### 写手可选提问
- 附言末尾可增加「📋 下章方向疑问：」区块
- 审查面板展示 `❓ 写手提问`，用户可在方向框中回答

### save_snapshot 安全修复
- 交换 `save_snapshot` 与 `save_state` 顺序，快照先落盘再更新 state.json

### 代码清理与工程化
- `_build_llm` 去重：consistency_checker 改为从 novelist import
- prompt 拼知识文件统一为 `_build_knowledge_sections()`
- 删除 novelist.py 末尾冗余 `estimate_tokens = lambda`
- `frequency_penalty`/`presence_penalty` 移到顶层参数，消除 LangChain warning
- 新增 `.gitignore`，`.env.example` 更新，`requirements.txt` 补充 transformers

### Web UI 改进
- 告警聚合：generate_chapter 收集 alerts，审查面板顶部红色告警区块
- 重写历史积累：多次打回原因在 prompt 中展示
- 启动超时从 30s 降至 5s，Web UI 先行弹出
- 状态栏恢复缓存命中率显示（`♻️ XXt (X%)`）
- 回退后自动检查 `_direction_chapter` 一致性并提示

### extractor 伏笔检查增强
- extractor prompt 传入当前 pending 伏笔列表，要求逐一检查是否在本章结案
- 防止"正文已处理但伏笔一直挂起"的连续性断裂

### 连续性修复
- 确认 ch12 被猎手队处理的蚀元兽与 ch25+ 遇到的为不同个体
- state.json/summaries.json 已修正标记

## 当前代码库状态
```
NovelNexus_V3/
├── config.py             (向量记忆配置 + DeepSeek API)
├── token_counter.py      (官方 DeepSeek tokenizer)
├── system_builder.py     (prompt 组装 + extractor/讨论/回忆规划)
├── novelist.py           (写作编排 + 向量检索 + 精确确认)
├── state_manager.py      (状态持久化 + 快照回退 + validate)
├── novel_memory.py       (ChromaDB 向量库 + 时序加权)
├── consistency_checker.py(一致性审查)
├── token_tracker.py      (调用计费 + 缓存追踪)
├── web_ui.py             (Gradio 前端)
└── deepseek_v3_tokenizer/(官方 tokenizer 文件)
```

