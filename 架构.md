# NovelNexus V3 架构

## 一、四层架构

```
┌─────────────────────────────────────────────────────────┐
│  Web Layer: web_ui.py (Gradio)                           │
│  写作/讨论/回退/小说管理/知识面板/记忆检索/一致性审查     │
└───────────────────────┬─────────────────────────────────┘
                        │ 函数调用
┌───────────────────────▼─────────────────────────────────┐
│  Logic Layer: novelist.py                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐          │
│  │ Novelist │  │ Extractor│  │ Compressor   │          │
│  │ (pro)    │  │ (flash)  │  │ (flash)      │          │
│  │ 写作/讨论 │  │ 摘要提取  │  │ 上下文压缩    │          │
│  └──────────┘  └──────────┘  └──────────────┘          │
│  + consistency_checker.py (flash LLM, 事实矛盾检测)      │
└───────────────────────┬─────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────┐
│  Memory Layer                                           │
│  ┌──────────────────┐  ┌──────────────────────────────┐ │
│  │ novel_memory.py  │  │ state_manager.py              │ │
│  │ 向量记忆 (V4新增) │  │ JSON/MD 文件读写              │ │
│  │ ChromaDB         │  │ · state.json 动态状态         │ │
│  │ sentence-transform│  │ · summaries.json 摘要链      │ │
│  │ ers 语义检索      │  │ · knowledge/*.md 设定        │ │
│  │ consistency_log   │  │ · snapshots/ 快照            │ │
│  └──────────────────┘  │ · discussion_log 讨论记录     │ │
│                        │ · physical_details 物品外貌   │ │
│                        └──────────────────────────────┘ │
└───────────────────────┬─────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────┐
│  Config Layer                                           │
│  · config.py: 模型参数/定价/路径/向量记忆开关             │
│  · token_tracker.py: 用量/费用追踪                      │
│  · system_builder.py: 各智能体 Prompt 构建              │
│  · .env: API Key + 环境变量                             │
└─────────────────────────────────────────────────────────┘
```

## 二、数据流

### 写作流
```
写作前自动检索(state角色名 → 向量库) → 方向(可选) + 手动检索(可选)
  → 注入Prompt(含检索原文) → Novelist(pro) → 正文
  → 标题审查(flash, 不匹配则自动重写)
  → 一致性审查(flash, 第3章起, 对比历史原文)
  → UI展示(正文+冲突提示+标题修正)
  → [接受] → Extractor(flash) → JSON提取 → 更新/写入5处
      ① output/第NNN章.md
      ② state.json（进度/角色坐标/伏笔）
      ③ summaries.json（摘要链）
      ④ knowledge/*.md（新角色/新结构）
      ⑤ novel_memory.py 向量索引
```

### 讨论流
```
用户输入 → 触发自动回忆(向量检索) → Novelist(pro)
  → 分析+方案+recommendation → UI展示
  → [确认执行] → 写 knowledge/*.md → 创建讨论前快照
```

### 回退流
```
用户选目标章节 → 删除中间 output + 快照
  → 恢复 state.json + summaries.json
  → [可选] 从 snapshots/disc_XXX_pre.json 恢复 knowledge/*.md
```

## 三、文件依赖

```
                     web_ui.py
                    /    |     \
             novelist.py  config.py  token_tracker.py
            /    |     \
system_builder.py  state_manager.py  novel_memory.py
                        |                |
                   knowledge/novels/   ChromaDB(SQLite)
                   .md + .json
```

## 四、4个 LLM 智能体

| 智能体 | 用途 | 默认模型 | Temperature |
|--------|------|---------|-------------|
| Novelist | 写作 + 讨论 | deepseek-v4-pro | 1.0 |
| Extractor | 章节摘要提取 | deepseek-v4-flash | 0.3 |
| Compressor | 上下文压缩 | deepseek-v4-flash | 0.3 |
| Reviewer | 一致性审查 + 标题审查（V4新增） | deepseek-v4-flash | 0.3 |

## 五、novels/ 目录结构

```
novels/<小说名>/
├── knowledge/               # 全部状态 + 设定
│   ├── 世界观.md             # 世界规则、力量体系
│   ├── 角色.md               # 角色设定
│   ├── 大纲.md               # 卷级弧线 + 里程碑
│   ├── 风格.md               # 写作风格偏好
│   ├── 续写规范.md            # 续写硬约束 + 已命名结构词典
│   ├── state.json            # 动态状态（进度/角色坐标/伏笔/悬念）
│   ├── summaries.json        # 章节摘要链
│   ├── discussion_log.json   # 讨论历史
│   ├── physical_details.json # 人物外貌/物品特征（V4新增）
│   ├── consistency_log.jsonl # 一致性审查记录（V4新增）
│   ├── style_progression.json# 风格演化记录
│   └── snapshots/            # 快照 + 事件时间线
├── output/                   # 第001章.md ~ 第NNN章.md
└── memory_db/                # ChromaDB 向量库（V4新增，启动自动生成）
```

## 六、关键设计原则

1. **知识文件即数据库**：所有持久化状态存为 `.md` + `.json` 文件，无需 SQL
2. **双 Tab 隔离**：写作只读 knowledge/ 文件，看不到讨论对话流
3. **五处同时落地**：接受一章同时更新 output/ + state.json + summaries + knowledge/ + 向量索引
4. **向量记忆兜底**：当 `PersistentClient` 不可用时自动降级为 `EphemeralClient`
5. **回退无损**：每次接受章节 + 讨论修改前自动创建快照，可任意回退
6. **三接口独立配置**：写作/提取/压缩各用独立模型和参数
