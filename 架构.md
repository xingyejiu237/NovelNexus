# NovelNexus 架构

## 系统概览

```
┌─────────────────────────────────────────────────┐
│  Web UI: web_ui.py (Gradio)                     │
│  写作标签 / 讨论标签 / 侧边面板                  │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│  编排层: novelist.py                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │ Writer   │  │Extractor │  │  Discusant   │  │
│  │ (pro模型) │  │(flash模型)│  │  (pro模型)   │  │
│  │ 写章节    │  │ 解析章节  │  │ 讨论剧情     │  │
│  └──────────┘  └──────────┘  └──────────────┘  │
│  + consistency_checker.py (flash, 一致性审查)    │
│  + token_counter.py (DeepSeek 官方 tokenizer)   │
│  + token_tracker.py (调用计费 + 缓存追踪)        │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│  记忆层                                          │
│  ┌──────────────────┐  ┌──────────────────────┐ │
│  │ novel_memory.py  │  │ state_manager.py     │ │
│  │ ChromaDB 向量库  │  │ JSON/MD 文件读写     │ │
│  │ 语义检索原文     │  │ · state.json 动态状态│ │
│  │ 时序加权排序     │  │ · summaries.json 摘要│ │
│  └──────────────────┘  │ · snapshots/ 快照    │ │
│                        │ · discussion_log     │ │
│                        │ · physical_details   │ │
│                        └──────────────────────┘ │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│  Prompt 层: system_builder.py                   │
│  组装 Writer/Discussant/Extractor/Compressor    │
│  的 System Prompt + Human Message               │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│  配置层: config.py                              │
│  模型参数 / DeepSeek API / 定价 / 向量记忆开关   │
└─────────────────────────────────────────────────┘
```

## 核心模块

```
NovelNexus_V3/
├── web_ui.py               Gradio 前端，写作/讨论/回退/知识面板
├── novelist.py              编排层：章节生成、章节提取接受、讨论、回退
├── system_builder.py        Prompt 组装：Writer/Discussant/Extractor/Compressor
├── state_manager.py         文件系统读写：state/summaries/snapshot/rollback
├── novel_memory.py          ChromaDB 向量库：章节索引、语义检索、索引重建
├── consistency_checker.py   章节正文与历史原文的矛盾检测
├── token_counter.py         DeepSeek 官方 tokenizer 封装
├── token_tracker.py         API 调用计费、缓存命中追踪
├── config.py                DeepSeek API 参数、定价、写作参数集中配置
└── deepseek_v3_tokenizer/   DeepSeek tokenizer 模型文件
```

## 小说文件系统

每部小说独立目录，以 `novels/<小说名>/` 为根。

```
novels/<小说名>/
├── knowledge/                       ← 全部状态与设定
│   ├── 静态设定文件（作者维护，不自动修改）:
│   │   ├── 世界观.md                世界规则、力量体系、地理设定
│   │   ├── 角色.md                  角色设定（姓名、能力、背景）
│   │   ├── 大纲.md                  卷级弧线、剧情里程碑
│   │   ├── 风格.md                  写作风格偏好、注意事项
│   │   └── 续写规范.md              续写硬约束、结构词典
│   │
│   ├── 动态状态文件（系统自动读写）:
│   │   ├── state.json               当前进度、角色位置/状态/装备、伏笔、悬念
│   │   ├── summaries.json           每章提取的150字摘要
│   │   ├── discussion_log.json      讨论会话记录与决策历史
│   │   ├── physical_details.json    角色外貌、物品外观（自动去重）
│   │   └── style_progression.json   风格演化记录（预留）
│   │
│   └── snapshots/                   快照与事件时间线
│       ├── ch_001.json ... ch_NNN.json  每接受一章自动创建
│       ├── disc_XXX_pre/post.json        讨论修改前后快照
│       └── events.jsonl                  操作审计日志
│
├── output/                        章节正文
│   └── 第001章.md ... 第NNN章.md     完整章节原文
│
└── memory_db/                     ChromaDB 向量库（启动自动生成）
    └── chroma.sqlite3
```

### 静态文件 vs 动态文件

| 类型 | 文件 | 维护者 | 用途 |
|------|------|--------|------|
| 静态 | 世界观.md / 角色.md / 大纲.md / 风格.md / 续写规范.md | 作者 | 写作 prompt 的基础设定上下文，每次写作全部加载 |
| 动态 | state.json | 系统 | 角色坐标、装备、伏笔状态、悬念——每次提取章节后自动更新 |
| 动态 | summaries.json | 系统 | 逐章摘要——写入 prompt 的【逐章摘要】区块，用于事实确认 Stage 2 |
| 动态 | physical_details.json | 系统 | 角色外貌物品特征——去重存储，防止前后矛盾 |
| 动态 | discussion_log.json | 系统 | 讨论决策摘要——写入 prompt 的【讨论决策摘要】区块 |
| 动态 | snapshots/ch_N.json | 系统 | 回退时的恢复点 |
| 动态 | snapshots/events.jsonl | 系统 | 操作审计日志，只追加不删除 |

## 数据流

### 写章节

```
用户输入方向 → generate_chapter()
  ├─ build_writer_prompt()       组装系统 prompt（静态文件 + 摘要 + 大纲）
  ├─ _plan_and_recall()          flash 模型规划写作方向 + 确定检索需求
  │    └─ _confirm_fact()        三级下探：向量语义 → 摘要关键词 → 原文搜索
  ├─ build_writer_human()        组装用户侧 prompt（角色坐标 + 伏笔 + 检索结果）
  ├─ Writer (pro)                生成正文
  ├─ _check_title()              flash 审查标题合理性
  ├─ check_consistency()         flash 审查与历史原文的矛盾
  └─ 返回正文 + 审查结果到前端

用户点击「接受并保存」→ accept_chapter()
  ├─ build_extractor_prompt()    传入待揭示伏笔列表
  ├─ Extractor (flash)           解析章节为 JSON
  ├─ 更新 state.json             位置/装备/伏笔/悬念/时间线
  ├─ 更新 summaries.json         追加摘要
  ├─ 保存 output/第NNN章.md
  ├─ save_snapshot()             创建快照（先快照后更新 state）
  ├─ novel_memory.index_chapter()索引入向量库
  └─ record_event()              追加审计日志
```

### 讨论

```
用户输入 → discuss()
  ├─ 向量检索相关原文片段
  ├─ build_discussion_prompt()   静态文件 + 摘要 + 检索结果
  ├─ Writer (pro)                分析 + 方案 + 推荐
  └─ 用户确认执行 → 修改 knowledge/*.md → 创建快照
```

### 回退

```
用户选择目标章节 → rollback_to_chapter()
  ├─ 加载 ch_N.json 快照
  ├─ 恢复 state.json + summaries.json
  ├─ 删除 output/ 中 > N 章的 .md 文件
  ├─ 删除 snapshots/ 中 > N 章的快照
  ├─ 可选：从 disc_pre 快照还原 knowledge/*.md
  └─ rebuild_index()             重建向量库
```

## LLM 智能体

| 智能体 | 用途 | 默认模型 | Temperature |
|--------|------|---------|-------------|
| Novelist/Writer | 章节生成 / 讨论 | deepseek-v4-pro | 1.5 |
| Extractor | 章节解析提取 | deepseek-v4-flash | 0.3 |
| Compressor | 上下文压缩 | deepseek-v4-flash | 0.3 |
| Reviewer | 一致性审查 / 标题审查 | deepseek-v4-flash | 0.3 |
| Planner | 回忆规划层（写前检索规划） | deepseek-v4-flash | 0.5 |

## 设计原则

1. **静态设定 vs 动态状态分离** — 作者维护的 .md 设定文件，与系统自动更新的 JSON 状态文件，分开存储、分开写入
2. **知识文件即数据库** — 所有持久化状态存为文件，无需数据库服务
3. **快照先于状态写入** — save_snapshot 在 save_state 之前执行，回退时数据一致
4. **向量记忆兜底** — ChromaDB PersistentClient 不可用时自动降级为 EphemeralClient
5. **逐章索引** — 每接受一章自动索引到向量库，新启动时后台重建
